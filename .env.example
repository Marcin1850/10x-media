SUPABASE_URL=###
SUPABASE_KEY=###
SUPADATA_API_KEY=###
OPENROUTER_API_KEY=###
# Only used by the offline `npm run grant-credits` operator script (never wired into the Worker).
# The service_role key from `npx supabase status` (local) or dashboard -> Settings -> API.
SUPABASE_SERVICE_ROLE_KEY=###