<!-- MANUAL-E2E-REPORT -->
# Manual E2E Acceptance: Generate and Save a Video Summary (S-01)

- **Plan**: `context/changes/generate-and-save-summary/plan.md`
- **Date**: 2026-07-23, resumed 2026-07-24 (sessions 1–6). **COMPLETE** — every manual acceptance item and post-review scenario (1.3–7.3, 10–24) passed. Session 5 closed Scenario 10's allow-list half; session 6 closed Scenario 24's LLM-deadline half; **6.7 Firefox** was closed by a manual user pass on 2026-07-24 (passed). Nothing manual remains for phases 1–7 (Phase 8 is a separate deploy-gated migration).
- **Scope**: Manual acceptance of the Progress manual items + post-review scenarios 10–24
- **Environment**: local Supabase stack (`supabase_db_10x-media`), dev server `npm run dev` on **http://localhost:4322/** (4321 was occupied), all five secrets set in `.env` + `.dev.vars`. Browser driven via claude-in-chrome (Chrome).
- **Test user**: `<test-user email redacted>` (`ded46f2b-ffa9-4d13-88b5-4a1159fe06e2`), balance manually set to 5 at start.
- **Fixtures**: short = `https://youtu.be/JF5cjNVYbBI` (transcript ~4.7k out, informational); long = `https://www.youtube.com/live/HRIlJ-fqJ9g` (transcript ~76,722 chars → confirm gate, cost 2).

## Result summary

| Item | Scenario | Result | Evidence |
|------|----------|--------|----------|
| 2.4 | `spend_credits(2)` −2 / returns balance; over-spend → −1, no change | ✅ PASS | psql: 5→3, `spend_credits(100)`→ −1, balance still 3 (rolled back) |
| 2.5 | `spend_credits` denied for `anon` | ✅ PASS | `ERROR: permission denied for function spend_credits` |
| 2.6 | `refund_credits` service_role-only | ✅ PASS | denied for `authenticated` & `anon`; `service_role` 5→6 (rolled back) |
| 4.4 | Normal → 200, Polish summary, cost 1, −1, row written | ✅ PASS | balance 5→4; reservation `settled` amount 1 w/ request_id; summaries +1; content Polish informational (`## Przegląd wideo`, 4715 chars), `reservation_id` linked |
| 4.5 | Long → 200, cost 2, −2 | ✅ PASS | balance 3→1; reservation `settled` amount 2 w/ request_id |
| 5.3 | Long w/o `allowLong` → 409 `requiresConfirmation`, cost 2, no spend | ✅ PASS | UI prompt "This is a long video (~76,722 chars). Generating costs 2 credits."; balance unchanged |
| 5.4 | Retry with `allowLong` → 200, −2 | ✅ PASS | "Generate anyway (2 credits)" → balance 3→1, summary landed |
| 5.5 | 1-credit → 409 then 402 (need 2, have 1) | ✅ PASS | at balance 1: prompt shown, then "Not enough credits — you need 2 but have 1."; no spend |
| 6.3 | Valid URL + submit → loading → Polish summary inline; −1 | ✅ PASS | loading spinner "Generating…"; inline card "SUMMARY · 1 CREDIT SPENT"; Markdown rendered (heading/bold/bullets); badge 4→3 |
| 6.4 | Invalid URL blocked with a message | ✅ PASS | inline red "Enter a valid YouTube video URL." + red border; no request, no spend (F20) |
| 6.5 | Long → confirmation → "Generate anyway (2 credits)" → −2 | ✅ PASS | inline card "SUMMARY · 2 CREDITS SPENT" (Polish, informational); badge 3→1 |
| 6.6 (part) | 0 credits blocks generation | ✅ PASS | at balance 0: button disabled, "You have no summary credits left. Generation is disabled." |
| 7.3 | Informational → Polish key-facts list | ✅ PASS (already checked) | re-confirmed live in 4.4/6.3 |
| Scenario 20 | Quote reuse on confirm = one Supadata fetch total | ✅ PASS | `transcript_fetch_attempts` timestamps 21:07 / 21:13 / 21:15; "Generate anyway" recorded **no** 4th attempt; quote reused |
| Scenario 23 / F24 | Quote consumed after generation commits | ✅ PASS | `transcript_quotes` 1→0 immediately after long summary persisted |
| F22 / F23 (both runs) | request_id present; reservation settled atomically | ✅ PASS | both reservations `settled` with non-null `request_id` |
| Scenario 10 (render) | Markdown rendered in the UI + allow-list stripping | ✅ PASS | headings/bold/bullets render correctly (re-confirmed live in 4.4/6.3); allow-list stripping now verified — see session 5 table below |
| Scenario 24 | LLM deadline fires → refund + 502, nothing persisted | ✅ PASS | forced via a temporary (reverted) `SUMMARY_TIMEOUT_MS` 300_000→1_000 shrink; `502` + reservation `refunded`, balance/summaries net-unchanged — see session 6 table below |

### UI / forced-failure scenarios — resumed 2026-07-24 (dev server on **4321**, balance reset to 5)

| Item | Scenario | Result | Evidence |
|------|----------|--------|----------|
| 1.3 | English config-missing banner | ✅ PASS | `OPENROUTER_API_KEY` unset → "Notice: Transcript/LLM is not configured — summary generation is disabled."; `SUPABASE_KEY` also unset → "Notice: Supabase is not configured — authentication features are disabled. See the configuration guide." — both banners + `docsLabel` fully English, rendered live |
| Scenario 11 | Unknown balance keeps submission enabled; real 0 still 402 | ✅ PASS | `user_credits` row deleted → badge "— CREDITS" + "Your credit balance is unavailable right now. You can still generate…"; Generate stays **enabled**; submit → 402 "You have no summary credits left"; **zero** `reserved` rows leaked; row restored |
| 4.7 (upstream half) / Scenario 16 | Forced upstream error → 502, balance net-unchanged; reservation refunded | ✅ PASS | invalid `OPENROUTER_API_KEY` → transcript fetched, debit reserved (amount 1, request_id `d61cab0f…`), `summarize` failed → UI "The transcript or summarization service failed. Please try again."; reservation status **`refunded`**, balance 5→5 net (debit then refund confirmed in `credit_reservations`) |
| Scenario 14 | In-flight confirmation race (F11) | ✅ PASS | (a) deterministic: long-video 409 prompt shown → editing URL **cleared the prompt immediately** (quote can't consent for another video) and re-surfaced F20's inline "Enter a valid YouTube video URL."; (b) live race: submitted long A, swapped URL to short B via scripted input mid-flight → A's late 409 **silently dropped** (no prompt for B, no error, loading cleared), balance unchanged at 5, no reservation |

> **Env note (fallback trap)**: `astro:env` resolves `.dev.vars` first and **falls back to `.env`** — commenting a key out of `.dev.vars` alone does *not* unset it; it must be removed from both files to trigger the config-missing banner. An *invalid* (vs. missing) key in `.dev.vars` does take effect, which is what the 502 scenario used.
> Residue: a live `transcript_quotes` row for `HRIlJ-fqJ9g` remains from the race test (10-min TTL, self-draining — harmless). `.env`/`.dev.vars` restored to normal config afterwards.

### DB-internal RPC scenarios — resumed 2026-07-23 (rolled-back psql transactions)

Run directly against the local stack (`docker exec supabase_db_10x-media psql`), each scenario in its own `begin … rollback` so nothing persists (post-run balances unchanged at 5 / 1, zero residual locks). Assertions raise loudly on mismatch; all passed. Script: `scratchpad/db_e2e.sql`.

| Item | Scenario | Result | Evidence |
|------|----------|--------|----------|
| Scenario 12 | Lock contention → 429 | ✅ PASS | `acquire_generation_lease` #1 → lease id; #2 → `NULL` (on-conflict-do-nothing) |
| Scenario 13 | Stale takeover + lease-id ownership | ✅ PASS | 20-min-old lock taken over → **new** lease minted; displaced holder's `release(old_id)` → `false` (no-op, successor survives); 3rd acquire blocked (`NULL`); owner's `release(new_id)` → `true` |
| Scenario 18 | `reconcile_reservation` classification (F16) | ✅ PASS | reserved **with** linked summary → `settled`, no refund; **without** → `refunded`, balance +2 |
| Scenario 19 | Transcript rate cap (F17) | ✅ PASS | `record_transcript_attempt(_,600,10)` allows 10, 11th → `false` (429); attempts aged past the 10-min window are pruned so a fresh one is allowed again |
| Scenario 21 | `persist_summary` atomicity (F23) | ✅ PASS | reserved → `persisted` + reservation `settled` in the same txn + summary linked; replay → `already_persisted`, same ids, no dup row; deleting the summary leaves billing `settled`; persist on a non-reserved (refunded) row → `not_reserved`, no write |
| Scenario 22 | Idempotent retry (F22) | ✅ PASS | probe fresh→`fresh`; debit→`reserved` (−1); probe/redebit while reserved→`in_progress` (no 2nd charge); after settle a debit-intent retry→`replay` (original content, balance intact); insufficient→actual balance 1 not 0 (F4); refunded key releases the slot so a retry is a genuine fresh reservation; null-key probe raises (caller bug) |
| Scenario 23 | Quote lifecycle (F24) | ✅ PASS | save→get hit; discard→miss; expired quote→miss + self-deletes on get; live quote survives; an abandoned expired quote is drained by an unrelated `save_transcript_quote` (self-limiting, no scheduled job) |

> Note: single-session psql validates each RPC's **decision logic** (conflict/`where balance>=amount`/`for update` gates). True cross-connection blocking on `for update` is serialized by construction (unique constraints + conditional updates) and is not separately exercisable in one connection.

### Endpoint-mapping & forced-failure scenarios — resumed 2026-07-24 (session 2, dev server on 4321, balance 5 at start)

Driven via `fetch()` executed in the signed-in dashboard tab's page context (claude-in-chrome), so requests carry the real session cookie without ever exposing it. Fixture: short video `https://youtu.be/JF5cjNVYbBI`.

| Item | Scenario | Result | Evidence |
|------|----------|--------|----------|
| Scenario 21/22 (endpoint mapping) | Idempotent replay → `200` with identical summary, no second charge | ✅ PASS | POST with `requestId a79af5c5…` → `200`, `summaryId e9a3d3fe…`, `cost 1`, balance 5→4; identical retry (same `requestId`) → `200`, **same** `summaryId`/summary length/`creditsRemaining: 4`; DB: exactly one `credit_reservations` row for that key, `status settled`, `amount 1`; balance unchanged at 4 after the retry |
| Scenario 17 (persistence-half) | Forced `persist_summary` failure after a successful, paid LLM call → `500` with the persistence-specific message, reservation refunded, nothing written | ✅ PASS | Added a temporary `NOT VALID` check constraint on `videos.youtube_id` (blocks only the fixture's row, doesn't scan/break existing data) to fail the video upsert inside `persist_summary`'s transaction; POST → **`500` "Something went wrong saving your summary. Please try again."** (the persistence copy, distinct from the generic pre-save 500); DB: reservation `refunded` (not stuck `reserved`, not `settled`), zero `summaries` rows for that reservation, balance net-unchanged at 4; constraint dropped afterward and `\d videos` confirmed removal |

> **Not independently observable via HTTP**: the idempotency-level `inProgress → 429` mapping in `respondToRepeatedRequest` (`src/pages/api/summaries/generate.ts:151-157`) is pre-empted in practice by the per-user generation lease, which acquires *before* the `requestId` probe runs and returns its own 429 first (already verified live as scenario 12, lock contention). Reaching the idempotency-level 429 via real concurrent HTTP requests would need two requests to race past the lease acquisition simultaneously, which isn't reproducible from a single client. The DB-level `in_progress` outcome itself is already proven by the psql script (scenario 22 in the table above); the code path that maps it to 429 is a 3-line `switch` case, reviewed by inspection.
> The **omit-`requestId`** (pre-F22 client) case was not re-run live — it is the exact pre-F22 behavior already exercised repeatedly in scenarios 4.4/4.5/6.3/6.5, and the schema/service code make `requestId` a no-op when absent (`generateSchema`'s `.optional()`, `beginGeneration`'s null-key bypass) — re-spending a real paid call to prove that again wasn't judged worth the cost.

### Forced-DB scenarios — resumed 2026-07-24 (session 3, dev server on 4321, balance 4 at start)

The two remaining scenarios that are forceable without a fixture or a code edit, driven the same way as session 2 (page-context `fetch()` in the signed-in dashboard tab) plus a reversible DB force. No paid Supadata/LLM calls; balance net-unchanged at 4 throughout.

| Item | Scenario | Result | Evidence |
|------|----------|--------|----------|
| 4.6 | Transcript > `HARD_MAX_TRANSCRIPT_CHARS` (200k) → 413 before any debit/LLM | ✅ PASS | injected a 200,001-char `transcript_quotes` row via `save_transcript_quote` (no paid fetch), POST `allowLong:true` reused it → **413 "This video's transcript is too long to summarize."**; balance 4→4, `credit_reservations` 9→9, `summaries` 14→14 (413 fires at the hard-cap gate `generate.ts:284`, *before* the 409/debit); injected quote `discard`-ed afterward (0 remaining) |
| 19 (fail-closed half) | `record_transcript_attempt` DB error → 500 (fails **closed**, not open) | ✅ PASS | temporary `check (false) not valid` constraint on `transcript_fetch_attempts` → the RPC's insert raises (confirmed by a rolled-back direct call); POST `allowLong:false` → `recordTranscriptAttempt` throws → **500 "Something went wrong. Please try again."** (the generic pre-fetch copy at `generate.ts:247`, distinct from the persistence 500); balance 4→4, reservations 9→9, summaries 14→14, **attempts unchanged at 2** (insert rejected → failed before any Supadata fetch or debit); constraint dropped, RPC confirmed recovered |

> **Technique note**: both forces are reversible DB-only manipulations — no code edit, no dev restart. 4.6 exploits that the quote path is read only on `allowLong:true`, so an oversized `transcript_quotes` row injects a >200k transcript without a Supadata call. 19 uses a `NOT VALID` check constraint, which enforces on new inserts only (existing rows unscanned), failing the RPC without touching stored data — same family as session 2's `videos.youtube_id` constraint for the persist-failure test.

### No-usable-transcript scenarios — resumed 2026-07-24 (session 4, dev server on 4321, balance 4 at start)

The last genuinely-untested code branches: the two `422` "no usable transcript" paths (`transcript.ok === false` at `generate.ts:266` and empty/whitespace content at `generate.ts:272`). Neither is reachable via a DB force — the quote-injection lever used in session 3 bypasses the real-fetch branch where both checks live. Forced instead with a **temporary, fixture-scoped mock** at the top of `fetchTranscript` (`src/lib/services/transcript.ts`) that intercepts three sentinel video ids and returns without any paid Supadata call; the edit was **reverted immediately afterward** (`git diff src/lib/services/transcript.ts` clean). Endpoint probes driven via page-context `fetch()` in the signed-in dashboard tab; 6.6b driven through the real form.

| Item | Scenario | Result | Evidence |
|------|----------|--------|----------|
| (mock liveness) | Sentinel returning >200k chars → 413 | ✅ PASS | `E2Elive0000` → **413 "This video's transcript is too long to summarize."** — an outcome a real fetch of a fake id can never produce, so this proves the mock module reloaded and the two 422 branches below are genuinely exercised (not a fake-id `ok:false` masquerading as the empty branch) |
| 4.7 (no-transcript half) / Scenario 15a | `transcript.ok === false` → 422, no debit | ✅ PASS | `E2Enotrans0` → **422 "Transcript unavailable for this video"** (`generate.ts:267`); latency ~174ms (mock, no Supadata round-trip); balance 4→4, reservations 9→9, summaries 14→14 |
| Scenario 15 (empty half) | empty/whitespace transcript → 422, no reserve, no LLM | ✅ PASS | `E2Eempty000` (mock returns `"  \n \t "`) → **422 "Transcript unavailable for this video"** via the whitespace branch (`generate.ts:272`), trustworthy because the 413 liveness probe confirms the mock is live; no debit/reservation/summary |
| 6.6 (2nd half) | no-transcript UI message + no spend | ✅ PASS | typed `https://youtu.be/E2Enotrans0` into the form → submit → red banner **"No transcript is available for this video."** (distinct from the network-failure copy), badge stays **4 CREDITS**; DB net-unchanged (balance 4, reservations 9, summaries 14) |

> **Technique note**: the mock keys on three sentinel 11-char youtube ids (`E2Enotrans0` → `ok:false`; `E2Eempty000` → whitespace `ok:true`; `E2Elive0000` → >200k `ok:true`), all valid per `extractYoutubeId` so they reach the transcript path but never a real video. Each 422/413 fires **before** the debit, so no credit is reserved; each real-fetch path still consumes one rate-limit token (`recordTranscriptAttempt` runs before the fetch), so `transcript_fetch_attempts` went 0→4 across the four submissions — expected, self-pruning after the 10-min window. The mock is the only forced-failure lever in this report that required a source edit (no DB lever exists for the real-fetch branch); it was reverted the moment the runs finished.

### Markdown allow-list scenario — resumed 2026-07-24 (session 5, dev server on 4321, balance 4 at start)

The last code-forceable item: Scenario 10's allow-list half (previously PARTIAL — img/link/raw-HTML stripping untested). The saved summary renders **inline from the generate endpoint's JSON body** (`result.summary` → the `react-markdown` island at `GenerateSummaryForm.tsx:378`), never from a persisted DB row, so the faithful, zero-cost force is a **page-context `window.fetch` override** that returns a crafted `{summary,…}` for `/api/summaries/generate` — the real form, real component state, and the real `allowedElements={SUMMARY_ALLOWED_ELEMENTS}` + `unwrapDisallowed` config all execute; only the untrusted summary text is controlled (which is exactly the attack input). No paid Supadata/LLM call, no DB write, balance untouched at 4. The crafted summary carried a markdown image, a markdown link, and raw-HTML `<img onerror=…>` / `<a href>` / `<script>` / `<b>`, plus allowed elements (h2/strong/list). Canary flags (`window.__canaryExec`, `__canaryScript`) detected any `onerror`/`<script>` execution; the interceptor was removed by a page reload afterward.

| Item | Scenario | Result | Evidence |
|------|----------|--------|----------|
| Scenario 10 (allow-list) | Summary with image/link/raw-HTML renders stripped/unwrapped, text still shows, no untrusted fetch | ✅ PASS | rendered container: **0** `<img>` elements (both markdown `![]()` and raw `<img>` gone), **0** `<a>` elements (markdown link + raw anchor gone), **0** live `<script>`/`<b>` from the raw HTML; `__canaryExec`/`__canaryScript` both stayed `undefined` → the `onerror` never fired and the `<script>` never executed; **no** network request to `untrusted-e2e-canary.example` (DOM has no img/anchor, so no fetch was ever attempted); allowed elements rendered fine (h2 heading, `<strong>`, 2 `<li>`); markdown **link text** "LINKTEXT-CANARY" still visible (unwrapped anchor keeps children); markdown **image alt** correctly not shown (image node unwraps to nothing); all raw HTML shown as **literal escaped text** (`<img …>`, `<a …>RAWANCHOR-CANARY</a>`, `<script>…</script>`, `<b>RAWBOLD-CANARY</b>` appear verbatim, confirming react-markdown's no-`rehype-raw` default). Screenshot captured. |

> **Technique note**: overriding `window.fetch` in the signed-in tab exercises the render allow-list without any real generation — the only faked value is the response `summary` body, i.e. the untrusted string the allow-list exists to defend against. This is the cheapest faithful lever for any pure-render assertion; it touches no DB and spends no credit. Reverted by a page reload (`window.fetch` restored, no persistence to undo).

### LLM-deadline scenario — resumed 2026-07-24 (session 6, dev server on 4321, balance 4 at start)

The last non-Firefox item: Scenario 24's LLM 300s deadline firing → refund + 502. Forced by temporarily shrinking `SUMMARY_TIMEOUT_MS` from `300_000` to `1_000` in `src/lib/services/llm.ts` (Astro dev hot-reloads the server module, so no manual restart — same live-edit technique session 4 used for the transcript mock) and driving the **real** short fixture through the endpoint via page-context `fetch()` in the signed-in dashboard tab. This is a **controlled experiment**: the short fixture reliably returns `200`/`settled` at the normal deadline (proven repeatedly in 4.4/6.3), so shrinking *only* the deadline turning it into a 502+refund isolates the deadline as both live and the cause. Real Supadata transcript fetch + real OpenRouter call (aborted ~1s in, before completion → negligible token cost). The edit was **reverted immediately afterward** (`git checkout` → `SUMMARY_TIMEOUT_MS` back to `300_000`; `git status src/` clean).

| Item | Scenario | Result | Evidence |
|------|----------|--------|----------|
| Scenario 24 | `summarize` exceeding the deadline aborts → refund + `502`, nothing persisted | ✅ PASS | two runs (bg + captured), both: transcript fetched → debit **reserved** (amount 1) → `summarize` aborted by `AbortSignal.timeout(1_000)` → **refund**. Captured run: **`502`** body `"The summarization service failed. Please try again."` (the `summarize`-failure path, `generate.ts:357`), latency **8255ms** = ~7s transcript fetch + ~1s deadline (proves the timer *fired* rather than the request hanging toward 300s); reservations `4a10ca1d…` & `8981cf1e…` both **`refunded`**; balance **4→4 net**, `summaries` **14→14** (no row written), `generation_locks` empty (lease released). Deadline restored to `300_000` and tree confirmed clean. |

> **Technique note**: shrinking the timeout constant is the only forced-failure lever in this report besides session 4's transcript mock that required a source edit (the deadline is a compile-time constant with no DB or request-level override); like that mock it was reverted the moment the runs finished. The shared refund+502 catch was already proven independently by scenario 16 (forced-upstream 502 → `refunded`); session 6 adds the one piece scenario 16 could not — the `AbortSignal.timeout` deadline itself firing and routing into that catch.

## Pending / not yet run

**Nothing pending for phases 1–7.** The last item, **6.7 Firefox** cross-browser, was completed by a manual user pass on 2026-07-24 (**passed** — claude-in-chrome is Chrome-only, so it could not be automated here). Every Progress manual item (1.3–7.3) and every post-review scenario (10–24) is ✅.

The only remaining plan work is **Phase 8** (drop the six superseded RPCs) — a deploy-gated contract migration, out of scope for this manual-acceptance report.

**No longer pending** (done 2026-07-24 session 4, see the no-usable-transcript table above): **4.7-no-transcript-half (422), Scenario 15 (empty→422), 6.6-2nd-half (no-transcript UI message)** — all three forced via a temporary, reverted fixture-scoped transcript mock. **Session 5**: **Scenario 10 allow-list half** — forced via a page-context `window.fetch` override returning a crafted summary; no paid call, no DB write. **Session 6**: **Scenario 24 (LLM deadline → refund + 502)** — forced via a temporary, reverted `SUMMARY_TIMEOUT_MS` shrink; `502` + reservation `refunded`, balance/summaries net-unchanged.

**DB-internal RPC decision logic** — ✅ DONE 2026-07-23 (see the DB-internal table above): **12, 13, 18, 19, 21, 22, 23** all verified via rolled-back psql. ✅ DONE 2026-07-24 (UI/forced-failure table above): **1.3, 11, 14, 16, 4.7-upstream**. ✅ DONE 2026-07-24 session 2 (endpoint-mapping table above): **17-persistence-half, 21/22-endpoint-mapping (replay-200 case; `inProgress`→429 confirmed pre-empted by the lease, not independently reproducible)**. ✅ DONE 2026-07-24 session 3 (forced-DB table above): **4.6 (413 via oversized quote), 19-fail-closed (500 via forcing constraint)**. ✅ DONE 2026-07-24 session 4 (no-usable-transcript table above): **4.7-no-transcript-half, 15, 6.6-2nd-half (422/UI via reverted fixture-scoped mock)**. ✅ DONE 2026-07-24 session 5 (Markdown allow-list table above): **Scenario 10 allow-list stripping (img/link/raw-HTML/script all inert, no untrusted fetch)**.

## Notes / observations (not defects)

- **Dev-server Vite re-optimization races**: on first hit of a lazily-imported dep group, Vite re-optimized and full-reloaded mid-request, dropping the client connection so the UI hung on "Generating…" even though the server **committed** the summary and settled the reservation (verified in DB). This is a dev-only artifact (hot-reload severs the socket); production has no such reload. Retrying after deps are optimized renders inline normally. Affected the very first short generation (`astro/env/runtime`, then `@openrouter/ai-sdk-provider, ai`).
- **Table name**: the rate-limit ledger is `public.transcript_fetch_attempts` (not `transcript_attempts`).
- Credit spend is via the reservation ledger end-to-end: `begin_generation` reserves, `persist_summary` settles in one transaction — both confirmed live.

## How to resume

1. Ensure Supabase up (`npx supabase status`) and dev server running; note the actual port.
2. Sign in as the test user; set a known balance via `update public.user_credits set balance = N where user_id = '…'`.
3. Continue with the "Pending / not yet run" list above. **The pure-DB RPC block (12,13,18,19,21,22,23) is DONE** (rolled-back psql, script since cleaned up with the session's scratchpad). **The forced-failure/UI block (1.3, 11, 14, 16, 4.7-upstream) is DONE 2026-07-24** (key-unset/invalid-key via `.env`+`.dev.vars` — remember the fallback trap — plus scripted mid-flight input swap). **The endpoint-mapping block (17-persistence-half, 21/22-replay) is DONE 2026-07-24 session 2** (page-context `fetch()` against the signed-in dashboard tab, plus a temporary `NOT VALID` check constraint to force a post-LLM persist failure — see the table above for the technique, reusable for future forced-failure tests). **The forced-DB block (4.6, 19-fail-closed) is DONE 2026-07-24 session 3** (oversized `transcript_quotes` row for the 413; a `check (false) not valid` constraint on `transcript_fetch_attempts` for the fail-closed 500 — both reversible, no code edit). **The no-usable-transcript block (4.7-no-transcript-half, 15, 6.6-2nd-half) is DONE 2026-07-24 session 4** (a temporary, reverted fixture-scoped mock in `fetchTranscript` intercepting three sentinel ids — one >200k liveness probe proved the mock live, the other two exercised the 422 branches; `git diff src/lib/services/transcript.ts` clean afterward). **Session 6 (2026-07-24) closed Scenario 24** (LLM deadline → refund + 502) via a temporary, reverted `SUMMARY_TIMEOUT_MS` 300_000→1_000 shrink, driven against the real short fixture through the signed-in dashboard tab — `502` + reservation `refunded`, balance/summaries net-unchanged, `llm.ts` restored and `git status src/` clean. **Only ONE item now remains: 6.7 Firefox** (manual pass; claude-in-chrome is Chrome-only). Test-user balance left at 4; residue is a couple of self-pruning `transcript_fetch_attempts` rows and two `refunded` `credit_reservations` rows from the session-6 forces (both harmless, net-zero on balance).
