---
project: "10xMedia"
version: 1
status: draft
created: 2026-06-11
updated: 2026-07-31
prd_version: 1
main_goal: speed
top_blocker: external
---

# Roadmap: 10xMedia

> Derived from `context/foundation/prd.md` (v1) + auto-researched codebase baseline.
> Edit-in-place; archive when superseded.
> Slices below are listed in dependency order. The "At a glance" table is the index.

## Vision recap

Watching YouTube videos is time-consuming — when regularly following informational and educational channels, the list of potentially interesting content grows faster than the available time. Existing tools summarize single videos but don't help choose WHICH video is even worth watching. 10xMedia turns a video (after pasting a URL and selecting the channel character: informational or educational) into a Polish-language summary on the basis of which the user decides whether to watch or skip it.

## North star

**S-01: user generates and saves a video summary** — the smallest end-to-end flow that proves the product's core (YouTube transcript → Polish summary tailored to the channel character) works. With the "speed" goal, this is the only slice that must land to validate the PRD's single success criterion (75% of summaries "good enough").

> "North star" here means the smallest end-to-end flow whose successful delivery proves the product's core hypothesis — placed as early as dependencies allow, because everything else only matters if this works.

## At a glance

| ID   | Change ID                 | Outcome (user can …)                                         | Prerequisites | PRD refs                      | Status   |
| ---- | ------------------------- | ------------------------------------------------------------ | ------------- | ----------------------------- | -------- |
| F-01 | video-summary-schema      | (foundation) video/summary tables with per-user RLS          | —             | Access Control, NFR, FR-006   | done        |
| F-02 | transcript-llm-probe      | (foundation) transcript→LLM path verified on the Worker      | —             | FR-005, NFR                   | done        |
| S-01 | generate-and-save-summary | paste URL + pick character → get and save a Polish summary   | F-01, F-02    | US-01, FR-003, FR-004, FR-005 | done        |
| S-02 | browse-summary-list       | browse the list of saved summaries                           | S-01, S-08    | FR-006                        | planned |
| S-03 | delete-summary            | delete a summary                                             | S-01          | FR-007                        | proposed |
| S-04 | delete-account            | delete their account and all associated data (GDPR)          | F-01          | Access Control, NFR (privacy) | done        |
| S-05 | summary-credits           | start with 5 credits; each generation spends one             | F-01          | — (cost guardrail)            | done        |
| S-06 | app-design-system         | use the app through a coherent, production-like UI           | S-02          | — (product polish)            | planned     |
| S-07 | persist-time-and-cost | (observability) each summary records how long it took, what it cost on both providers, and keeps its transcript | S-01 | — (F-02 follow-ups F4/F5)  | done — live in production 2026-07-30, 5/5 phases verified |
| S-08 | persist-video-metadata    | see the video's thumbnail, title, channel, length, upload date and language next to a summary | S-01          | FR-006 (list legibility)      | done — impl-reviewed, 27/27 verified |
| S-09 | transcript-cost-guardrail | (guardrail) no single video can silently drain the monthly Supadata budget | S-01          | — (cost guardrail)            | **done — all 7 phases live in production 2026-08-06** (Worker `393417a8`, six migrations pushed); live pass cost **4 credits**, reconciled exactly, and the repeat generation moved `usedCredits` by **0** |
| S-10 | transcript-injection-guardrail | (guardrail) a transcript that fails — or cannot pass — an injection check never reaches the LLM | S-01, S-09    | — (integrity guardrail)       | proposed |

## Streams

Navigation aid — groups items that share a Prerequisites chain. Canonical ordering still lives in the dependency graph below; this table is the proposed reading order across parallel tracks.

| Stream | Theme                          | Chain                             | Note                                                                                          |
| ------ | ------------------------------ | --------------------------------- | --------------------------------------------------------------------------------------------- |
| A      | Core: transcript→summary       | `F-02` → `S-01` → `S-08` → `S-02` / `S-03` | Front-loads the top risk (external) per the "speed" goal. `S-08` gates `S-02` so the list ships with video metadata; `S-03`, `S-07` and `S-09` run parallel after `S-01` and do not wait on `S-08`; `S-10` follows `S-09`, since both add gates to the same pre-LLM stretch. The former caveat here — that `S-09` was cheapest *before* `S-08` because the duration pre-gate would relocate the metadata call — **lapsed 2026-07-31**: lever A was chosen and the call does not move (see §S-09 Decisions). |
| B      | Data & privacy                 | `F-01`                            | Minimal schema + RLS; joins Stream A at `S-01`.                                                |

## Baseline

What's already in place in the codebase as of `2026-06-11` (auto-researched + user-confirmed).
Foundations below assume these layers exist and do NOT re-scaffold them.

- **Frontend:** present — Astro 6 + React 19; auth forms, layout, dashboard (`src/pages`, `src/components`).
- **Backend / API:** present — Astro SSR endpoints for auth (`src/pages/api/auth/{signin,signup,signout}`).
- **Data:** partial — Supabase configured (`supabase/config.toml`), only `auth.users` used; no migrations or domain tables (`videos`/`summaries` do not exist).
- **Auth:** present — full sign-up/sign-in/sign-out/callback flow + middleware; deployed and verified end-to-end (`deploy-plan.md`). Covers FR-001, FR-002.
- **Deploy / infra:** present — Cloudflare Workers live (`10x-media.nightshiftlab.workers.dev`); CI auto-deploy on merge to `master`.
- **Observability:** present (basic) — `observability.enabled` in `wrangler.jsonc` + `wrangler tail`.
- **AI / LLM:** absent — no AI SDK and no transcript-fetch path; FR-005 not implemented (see `F-02`).

## Foundations

### F-01: Data schema for videos and summaries + RLS

- **Outcome:** (foundation) `videos` and `summaries` tables exist in Supabase with per-user RLS policies; multi-tenant privacy enforced at the database level.
- **Change ID:** video-summary-schema
- **PRD refs:** Access Control, NFR (data privacy), FR-006
- **Unlocks:** S-01, S-02, S-03; reduces the privacy-guarantee risk (the PRD's only guardrail) before any user data lands in the database
- **Prerequisites:** — (auth present in baseline)
- **Parallel with:** F-02
- **Blockers:** —
- **Unknowns:** —
- **Risk:** Low risk. Sequenced early because every data slice assumes these tables, and skipping RLS here would break the privacy guarantee. Scope capped to two tables + policies — it does not build "the whole data layer"; S-01 still integrates this layer through real summary writes.
- **Status:** done (implemented + reviewed; change `video-summary-schema`, Linear MAR-5). Not yet archived.

### F-02: Transcript→LLM path verified on the deployed Worker

- **Outcome:** (foundation) AI provider wired in (key as a Workers Secret), and the "fetch YouTube transcript → call LLM" path is verified on a live, deployed Worker; confirmed whether the free Cloudflare plan is sufficient or a paid plan / workaround is needed.
- **Change ID:** transcript-llm-probe
- **PRD refs:** FR-005, NFR
- **Unlocks:** S-01; reduces the top risk (external: YouTube blocking Cloudflare datacenter IPs + the free Cloudflare plan's CPU limit)
- **Prerequisites:** — (deploy present and verified in baseline)
- **Parallel with:** F-01
- **Blockers:** —
- **Unknowns:**
  - Can the YouTube transcript be fetched from Cloudflare datacenter IPs (not just the dev machine)? — Owner: user/team. Block: no (this spike is what resolves it).
  - Will the free Cloudflare plan (~10ms CPU limit) handle transcript parsing, or is a paid plan / Railway escape-hatch required? — Owner: user. Block: no.
- **Risk:** The riskiest element of the whole roadmap (top risk = external). Sequenced as the first de-risk: with the hard deadline of 2026-07-31 and after-hours work, any blocker on the live Worker must be discovered while schedule slack still exists. `infrastructure.md` names Railway (`@astrojs/node`) as the documented workaround if workerd / IP-blocking fail.
- **Status:** done (implemented + impl-reviewed; change `transcript-llm-probe`, Linear MAR-6 Done 2026-07-10). De-risk verdict: free Cloudflare plan sufficient — Railway escape hatch not needed. Not yet archived. Follow-ups carried to S-01: LLM prompt-engineering, transcript-length guard, upstream-error handling.

## Slices

### S-01: User generates and saves a video summary

- **Outcome:** the user pastes a YouTube video URL, selects the character (informational or educational), and gets a Polish summary tailored to the character, which is then saved.
- **Change ID:** generate-and-save-summary
- **PRD refs:** US-01, FR-003, FR-004, FR-005
- **Prerequisites:** F-01, F-02
- **Parallel with:** —
- **Blockers:** —
- **Unknowns:**
  - How to handle a video without an available transcript (user-facing message, alternative source)? — Owner: user. Block: no.
  - What exact YouTube URL validation rules to apply, and how to communicate validation errors? — Owner: implementation. Block: no.
- **Risk:** This is the north star — the slice that validates the product's core. It depends on F-02: if the transcript→LLM path fails on the chosen plan, this slice must be re-planned (workaround / Railway) before UI work starts. The open questions about missing transcripts and URL validation do not block planning — they have sensible default fallbacks (a message, input-side validation).
- **Status:** done — all 8 phases live in production 2026-07-24 (Linear MAR-7 → Done); pending `/10x-archive`. 8-phase plan written 2026-07-18, extended to 8 phases 2026-07-19 (`context/changes/generate-and-save-summary/plan.md`). Approach: productionize the F-02 probe pipeline (rename `probe`→`/api/summaries/generate`), add a dashboard generation UI, carry F-02 follow-ups (prompt-engineering, transcript-length guard, upstream-error handling — **but not F4/F5, see the note at the end of this status**), and add a variable-cost path (>40k transcript chars ⇒ 2 credits via new `spend_credits(amount)` RPC + `allowLong` confirmation). Testing manual-only (Module-3 deferral). **Plan-reviewed + hardened 2026-07-18→19** (RETHINK → SOUND, all 7 findings triaged/fixed): credit flow reworked to **debit-before-LLM + refund-on-failure** (new `refund_credits` service_role RPC) to close a concurrency budget bypass; migration made **expand-only** (drop of `spend_credit()` moved to a contract migration to avoid a deploy window). The former `S-01-fu` (`drop-spend-credit-contract`) is now **Phase 8** of this slice — a deploy-gated contract migration that drops `spend_credit()` only after the phases 1–7 Worker is live on cloud; added `HARD_MAX_TRANSCRIPT_CHARS` → 413 cap. **Implementation 2026-07-19 (branch `generate-and-save-summary`): phases 1–7 of 8 landed** — P1 English UI copy (`4f48536`), P2 variable-cost `spend_credits`/`refund_credits` migration + credits service (`23d59cc`), P3 cost-policy helper (`b22d6f1`), P4 endpoint rename & harden → `/api/summaries/generate` + 502/413 (`9072baa`), P5 `allowLong` 409 confirmation gate (`17817f2`), P6 dashboard generation island (`10b3ffa`), P7 prompt engineering + **Markdown rendering** (summaries now emit Markdown, rendered via `react-markdown` in the island — amends the original "no markdown rendering" non-goal; `af3bea3`); automated checks green throughout, P7 manual quality pass passed on both characters. DB-level manual checks (spend/refund grants) + P1 banner still to spot-check. **Only Phase 8 remains — deploy-gated**: the contract migration dropping legacy `spend_credit()` applies to cloud only after the phases 1–7 Worker is live. **Incomplete follow-up carry (noted 2026-07-20):** the F-02 Phase 4 impl-review deferred **five** items to S-01, not the three listed above — the two missed are F4 (persist `generation_ms`) and F5 (persist `cost_usd` via OpenRouter usage accounting), `context/changes/transcript-llm-probe/reviews/impl-review-phase-4.md:57-75`. Neither appears in S-01's plan, `src/`, or `supabase/migrations/`; S-01 treated latency and cost only as things to *bound* (the `HARD_MAX_TRANSCRIPT_CHARS` → 413 cap), never to *record*. Both are now tracked as **S-07** (`summary-generation-telemetry`) rather than reopening S-01 — adding a migration here would contend with the deploy-gated Phase 8. **Update 2026-07-24:** phases 1–7 **shipped to production** (merged to `master`, Worker deployed via `wrangler deploy`, the 9 expand migrations pushed to prod Supabase; manual e2e acceptance passed) — this satisfied Phase 8's deploy gate. **Phase 8 is now implemented + committed** (`9085d03`): `20260724120000_drop_legacy_rpcs.sql` drops all six superseded RPCs (widened from the original single `spend_credit()` drop to also cover `spend_credits`/`refund_credits`/`reserve_credits` and the two `generation_lock` fns), and the dead `reserveCredits` wrapper is removed; automated gates + local-DB verification green. **Phase 8 shipped to production 2026-07-24:** merged fast-forward to `master` (`f415f12..c0469d5`), Worker redeployed via CI run 30128397834 (`ci` + `deploy` green), then `supabase db push --linked` applied `20260724120000_drop_legacy_rpcs.sql`; `migration list --linked` in sync. Cloud checks 8.5–8.7 all closed — the deployed Worker had **no call site** for any dropped RPC (`reserveCredits` was defined but uncalled), a production `supabase db dump` shows all six functions absent (definitions *and* grants), and `settle_reservation`/`reconcile_reservation` remain `service_role`-only. The live normal/long generation smoke test was **accepted without re-execution** (user decision 2026-07-24): the drops are structurally verified and the generation path is covered by `reviews/manual-e2e-2026-07-23.md`. Impl-review triage complete (`reviews/impl-review-phase-8.md`, verdict APPROVED, F1 + F2 both FIXED). **All 8 phases live; ready for `/10x-archive`** — Linear MAR-7 → Done 2026-07-24.

### S-02: User browses the list of summaries

- **Outcome:** the user browses the list of their saved summaries.
- **Change ID:** browse-summary-list
- **PRD refs:** FR-006
- **Prerequisites:** S-01, S-08
- **Parallel with:** S-03
- **Blockers:** —
- **Unknowns:** —
- **Risk:** Low risk. After S-01 the data already exists; this visible list surface is also the mechanism by which the user judges the "75% good-enough summaries" criterion over time. Sequenced after S-01 because without saved summaries there is nothing to browse, and after S-08 so the list renders thumbnails, titles, channel names, lengths and upload dates from the start rather than shipping a bare-URL list that is immediately retrofitted.
- **Status:** **planned 2026-08-07** — 4-phase plan + brief in `context/changes/browse-summary-list/`, branch `browse-summary-list`. **No migration, no RPC, no change to the paid generation path**: both tables kept `select` for `authenticated` behind owner-scoped RLS after the single-writer migrations, so an RLS-scoped read is the whole data layer. Planning settled seven things the scope notes left open. **(1) `/dashboard` becomes the list** (user decision, over a dedicated `/summaries` route) — so the page widens past today's `max-w-lg` card. **(2) Flat list, one card per summary**, not grouped by video: `persist_summary` appends unconditionally, so a video holds one summary per character plus one per regeneration, and grouping would hide paid work. **(3) Preview + inline expand**, no detail route — matches the PRD's own reading model (opening sentences serve the watch/skip call, the body serves the reader skipping the video). **(4) Generation moves into a freely dismissable dialog**, which forces the slice's one structural change: the fetch lifecycle, idempotency key, confirm quote and staleness guard lift out of `GenerateSummaryForm` into a `useGenerateSummary` hook on a parent island, because an unmounted island cannot finish its own paid request. Phase 3 is that lift and carries **no new behaviour** by design — it is where the whole slice's regression risk sits. **(5) In-progress generations render as a pending card in the list** (user request); pending state is client-session-only, so a reload mid-generation drops the spinner — accepted, since the server lease and idempotency ledger already prevent a double charge. **(6) A new `GET /api/summaries`** re-read on success, because the generate response carries `summaryId`/`videoId` but no metadata, so a prepended card built from it would not match the same card after a reload; S-03 reuses the endpoint. **(7) No telemetry on the card** — `cost_usd` is provider spend, not the credit the user paid. Two facts verified live against the local stack during planning rather than assumed: the PostgREST embed **does** resolve across the composite `(video_id, user_id)` FK as a to-one object, and filtering on an embedded column nulls the embed instead of dropping the parent row (`!inner` would be needed). The 16 seeded local summaries already contain every edge case the plan tests — two all-null-metadata rows, one video summarised under both characters, and `vi_webp/…webp` plus signed-param (`?sqp=…&rs=…`) thumbnails. Next: `/10x-implement browse-summary-list phase 1`.

### S-03: User deletes a summary

- **Outcome:** the user deletes a summary from their list.
- **Change ID:** delete-summary
- **PRD refs:** FR-007
- **Prerequisites:** S-01
- **Parallel with:** S-02
- **Blockers:** —
- **Unknowns:** —
- **Risk:** Lowest priority (FR-007 = nice-to-have). With the "speed" goal it's the first candidate to defer if the 2026-07-31 deadline presses; kept as a small slice (not in Parked) because it's cheap and closes the CRUD on summaries.
- **Status:** proposed

### S-04: User deletes their account (GDPR)

- **Outcome:** the user permanently deletes their account, and all associated data (videos, summaries, credits) is removed — satisfying the GDPR right to erasure.
- **Change ID:** delete-account
- **PRD refs:** Access Control, NFR (data privacy)
- **Prerequisites:** F-01
- **Parallel with:** —
- **Blockers:** —
- **Unknowns:** resolved during planning —
  - ~~Hard delete vs. soft-delete + purge window?~~ → **hard delete** (immediate, irreversible; no scheduler).
  - ~~How to remove the Supabase `auth.users` record from an SSR endpoint (service-role key vs. client)?~~ → **service-role admin client** calling `auth.admin.deleteUser`; domain rows purge via existing cascade FKs.
- **Risk:** Low-to-medium risk. Depends on F-01 so the domain tables exist to cascade from; the auth account itself already exists in the baseline. Sequenced independently of the summary CRUD — it's a compliance guardrail, not part of the core loop. The main care point is completeness (no orphaned rows) rather than complexity.
- **Status:** done — merged to `master` 2026-07-13 (merge commit `78bca68`, pushed to origin), Linear MAR-10 → Done; pending `/10x-archive`. Both phases implemented + committed 2026-07-12 — Phase 1 `2ebeaaf`, Phase 2 `2b0c78e`, epilogue `2710a3c`; `SUPABASE_SERVICE_ROLE_KEY` set as a Cloudflare Workers Secret. Manual E2E verification **passed** (commit `546e8f0`). Impl-review ran 2026-07-12 — verdict **NEEDS ATTENTION** (0 critical, 2 warnings, 3 observations). Triage complete 2026-07-13 (commit `57851b0`): F1/F3/F4 fixed (server-side delete confirmation, masked error contract, shadcn Dialog a11y), F2 skipped (pre-existing baseline lint drift owned by a parallel branch), F5 = tracker sync; lint + build green on the feature files.

### S-05: New users start with a credit budget

- **Outcome:** each new user starts with 5 credits; generating a summary spends one credit, and generation is blocked at zero credits. Refilling credits is manual-only for now (no self-serve top-up).
- **Change ID:** summary-credits
- **PRD refs:** — (no direct PRD ref; roadmap-originated cost guardrail on FR-005's paid transcript/LLM pipeline)
- **Prerequisites:** F-01
- **Parallel with:** S-04
- **Integrates with:** S-01 (spend-enforcement wires into the generation endpoint)
- **Blockers:** —
- **Unknowns:** resolved during planning (2026-07-11) —
  - ~~Where does the balance live — column on the user profile vs. a dedicated `credits` table / ledger?~~ → **dedicated `user_credits` table** (PK → `auth.users` on delete cascade; read-only RLS; no client writes).
  - ~~What is the manual refill mechanism (direct SQL, Studio, admin script)?~~ → **offline `grant-credits.mjs` service-role script** (`npm run grant-credits -- <email> <n>`).
  - Enforcement model: **spend-on-success + up-front read gate** (no refund, no client-reachable increment, no service-role in the request path); atomic `SECURITY DEFINER spend_credit()`; seed via trigger on `auth.users` insert + backfill.
- **Risk:** Low-to-medium risk. Guards the OpenRouter budget against runaway generation. The storage/seeding/refill half depends only on F-01 and can be built independently; only the spend-and-block enforcement needs a call site, which lives in S-01's generation endpoint (and must be server-side so it can't be bypassed from the client). Because an un-credited S-01 exposes the budget, this slice should land **before or together with** S-01, not after it. Manual-only refill keeps scope small for the MVP.
- **Status:** done — merged to `master` 2026-07-14 (`482b686`), CI green and the Worker auto-deployed. Formal `/10x-impl-review` landed **NEEDS ATTENTION** (2026-07-13: 5 warnings + 3 observations); **all 8 triaged and fixed 2026-07-14** in `10f91f4`, every dimension now PASS (change `summary-credits`, Linear MAR-11). All 4 phases implemented and manually verified. **All 6 DB migrations are live on the cloud project** as of 2026-07-14 (`ukbptccdffiigkdekzcn`) — local and remote histories match, and the post-push privilege state is verified by `db dump`. Pending `/10x-archive`. Enforcement wires into the F-02 probe endpoint now; S-01 reuses the same `credits` service. **Note for future slices:** the push revealed that cloud default privileges grant `ALL` on new tables to `anon`/`authenticated`; `20260714140000` revoked the `anon` half schema-wide, but `authenticated` still gets CRUD on any new table by default — tighten per-table as `user_credits` does.

### S-06: Production-like application design

- **Outcome:** the user works through a coherent, production-like UI instead of the starter template — consistent layout, navigation, typography and states (loading / empty / error) across the auth, generation and summary-list surfaces.
- **Change ID:** app-design-system
- **PRD refs:** — (no direct PRD ref; product-polish slice on the existing UI surfaces)
- **Prerequisites:** S-02
- **Parallel with:** —
- **Blockers:** —
- **Unknowns:**
  - How far to go — restyle the existing shadcn/ui surfaces vs. adopt a fuller design language (colors, spacing scale, brand)? — Owner: user. Block: no.
  - Which surfaces are in scope beyond auth + generation + list (e.g. landing page)? — Owner: user. Block: no.
- **Risk:** Low risk, but ordering matters more than size here. This slice's value is coherence *across* surfaces, so it must run after the surfaces exist: S-01 builds generation, S-02 builds the list. Running it earlier means styling screens that don't exist yet, and contending for the same components in parallel worktrees. S-03 is deliberately not a prerequisite — it lands whenever it lands and inherits the design rather than re-opening this slice. Scope stays presentational — no behavior changes to generation, credits or CRUD. Under the "speed" goal it is deferrable alongside S-03 — polish, not a PRD requirement.
- **Scope notes:**
  - **Browser password generator on the sign-up page (noted 2026-07-30):** part of "production-like" is that the browser's built-in *suggest strong password* flow works on `/auth/signup`. It currently cannot fire — no `autocomplete` attribute is set anywhere in the auth forms (`FormField` renders a bare `<input>`, `src/components/auth/FormField.tsx:42-55`; the only `autoComplete` in `src/` is the `"off"` on the delete-account confirmation), so Chrome/Safari/Firefox get no `new-password` signal to attach a generator or a save-to-manager prompt to. Minimum change: `autocomplete="username"` on the email field, `autocomplete="new-password"` on both password fields (`src/components/auth/SignUpForm.tsx:81,104`), and `autocomplete="current-password"` on the sign-in password field so the two forms aren't confused for each other. **Watch out for the show/hide toggle:** it swaps the input's `type` from `password` to `text` (`SignUpForm.tsx:84,108`), and a `text` input is not a password field as far as the generator is concerned — the affordance can vanish while the password is visible, so verify both states. Cheap and presentational-adjacent; belongs in this slice rather than a slice of its own.
- **Status:** planned

### S-07: Summary generation telemetry (latency, cost, transcript)

- **Outcome:** (observability) every saved summary records how long generation took and what it cost on **both** paid providers — OpenRouter in dollars, Supadata in credits/API calls — and the transcript it was generated from is kept, so models, characters and transcript branches can be compared on speed, spend and input size — not just on content.
- **Change ID:** persist-time-and-cost (renamed from `summary-generation-telemetry` when the change folder was opened 2026-07-28)
- **PRD refs:** — (no direct PRD ref; carries F-02 impl-review findings F4 and F5, and feeds the PRD's Open Q2 "75% good-enough" tracking mechanism)
- **Prerequisites:** S-01. T4 (Supadata spend) additionally assumes S-08 is live — it is, deployed 2026-07-26 — because the second Supadata call and `videos.duration_seconds` both come from there.
- **Parallel with:** S-02, S-03
- **Blockers:** —
- **Unknowns:**
  - Single wall-clock `generation_ms` vs. split `transcript_ms` / `llm_ms`? — Owner: implementation. Block: no. (Split is more diagnostic given the 33 s vs. 254 s spread came from the *transcript* branch, not the LLM.)
  - Persist token counts alongside `cost_usd`, or cost only? — Owner: implementation. Block: no.
  - Reuse a stored transcript on re-generation, or always re-fetch? — Owner: user. Block: no. (Reuse saves Supadata cost and closes S-01's accepted double-fetch, but serves a stale transcript if the video's captions later change. A `fetched_at` column keeps the option of a staleness window open without deciding now.)
  - Retention: keep transcripts indefinitely, or purge after N days? — Owner: user. Block: no. (Indefinite is simplest and matches MVP scale; the erasure path is already covered — see Scope notes.)
  - **Supadata spend: record it per *summary*, or per *generation attempt*?** — Owner: user. Block: no. (Columns on `summaries` follow F4/F5 exactly and are near-free, but they are written only when a summary is persisted — so a 422 `transcript-unavailable` (1 billable credit), a refused/errored generation and the metadata retry's second credit all stay invisible, and those are precisely the wasted spend. An append-only `supadata_calls` ledger row per call catches all of it. Recommendation: ship columns first, note the blind spot, promote to a ledger only if the numbers stop adding up against `GET /v1/me`.)
  - **Derive credits from the price table, or read the vendor's own counter?** — Owner: implementation. Block: no. (Supadata returns no per-request cost in its response — unlike OpenRouter's `usage.cost` — so cost is either computed locally from the documented table or observed via `GET /v1/me`'s `usedCredits`, which is **free to call**. The counter is ground truth but org-wide, so concurrent generations interleave and a delta can be mis-attributed. Recommendation: count calls locally as the record of truth per row, and take a `usedCredits` reading as calibration, not as the per-row value.)
- **Risk:** Low risk — additive nullable columns on `summaries` (F4/F5/T4) and `videos` (T3), no backfill, no RLS change, same idempotent-migration pattern already used twice for `model` and `resolved_via`. All four parts land in one migration; T4 is the only one that touches the endpoint rather than a single service, and it adds a counter, not a decision — nothing in the generation path branches on it. The only part that is more than bookkeeping is T3's optional reuse-on-regeneration path, which changes generation behaviour — it is listed as an open question and can ship as store-only first. **Provenance:** these were deferred to S-01 by the F-02 Phase 4 impl-review (`context/changes/transcript-llm-probe/reviews/impl-review-phase-4.md` F4 `:57-65`, F5 `:67-75`) but were **not carried into S-01's plan** — S-01 reasoned about latency and cost only as things to *bound* (the `HARD_MAX_TRANSCRIPT_CHARS` → 413 cap), never to *record*. The omission was found 2026-07-20, after S-01 phases 1–7 landed; verified absent from `src/`, `supabase/migrations/` and S-01's plan. Sequenced after S-01 rather than folded into it so the deploy-gated Phase 8 contract migration is not disturbed.
- **Scope notes:**
  - **F4 (latency):** additive `generation_ms integer` (or split `transcript_ms`/`llm_ms`) on `summaries`; thread timing through the persistence service. Motivation: the live F-02 run measured 33 s (inline transcript + LLM) vs. 254 s (async-job poll to exhaustion) — a wide, meaningful spread that is currently unrecorded.
  - **T3 (transcript persistence):** additive nullable `transcript text` + `transcript_fetched_at timestamptz` on **`videos`**, not `summaries` — a video carries one transcript but can have two summaries (one per character; the local DB already has such a pair for `dQw4w9WgXcQ`), so `summaries` would duplicate the text per character. Input size then comes free as `length(transcript)`, directly comparable to `LONG_TRANSCRIPT_CHARS`/`HARD_MAX_TRANSCRIPT_CHARS` (`src/lib/services/summaries.ts:84-85`), which are also char-based. **Motivation (measured 2026-07-20):** the S-01 F1 triage asked how output length compares to input and the question was unanswerable — `summaries` records `content` but nothing about the input. Output measured ~4.2–4.9k chars across 4 rows and is effectively constant in cost (~1.8 ¢/summary at Sonnet 5 intro rates), while input spans ~50× and dominates the bill; without the transcript, the ratio that matters cannot be computed. **Secondary win:** closes S-01's explicitly accepted "double transcript fetch on confirm" tradeoff (plan.md §Critical Implementation Details) — the 409 → "Generate anyway" resubmit currently re-fetches, occasionally re-triggering a paid Whisper job. **Erasure:** no new GDPR work — `videos.user_id` already carries `on delete cascade` to `auth.users` (`20260613145120_videos_and_summaries.sql:6`), so S-04 account deletion removes transcripts with the rest. RLS: reuse the existing per-user `videos` policies; the new columns need no separate policy. **Sizing:** bounded by `HARD_MAX_TRANSCRIPT_CHARS` at 200k chars/row worst case — negligible at MVP scale, worth revisiting only if the user cap lifts.
  - **F5 (cost):** add `usage: { include: true }` to the model options in `getSummaryModel` (`src/lib/services/llm.ts:7-9`, currently no options object), return `providerMetadata?.openrouter?.usage` from `summarize()`, persist to an additive `cost_usd numeric`. Feasibility already confirmed against `@openrouter/ai-sdk-provider` via Context7 — no new dependency. `cost` is `number | undefined`, so persist null-safely.
  - **T4 (Supadata spend — added 2026-07-27):** F5 covers only the OpenRouter half of the bill. The *other* paid provider is currently unmeasured, and since S-08 it is the one with two call sites per generation. Persist how many Supadata API calls a generation made and what they cost in credits — additive `supadata_calls integer` + `supadata_credits integer` on `summaries` (placement is the open question above). **Why it isn't just F5 again:** Supadata's response body carries no cost field, so credits have to be attributed by *operation type*, from the price table already captured in `context/changes/persist-video-metadata/docs/supadata-transcript.md` §Pricing + `supadata-metadata.md` §Pricing — metadata **1 credit flat**, native transcript **1**, generated (Whisper) **2 credits per minute of video**, a `206 transcript-unavailable` **1** (billable), job polling **free**. So a call count alone is not a cost: one `job`-resolved call can be worth 100 metadata calls. Record **both** — the count is the raw fact the user asked for and is exact; the credit figure is a derived estimate and should be commented as such.
    - **Attribution the code already supports:** `summaries.resolved_via` ('inline' | 'job', `20260709120000_add_resolved_via_to_summaries.sql:9`) distinguishes the cheap path from the Whisper path, and S-08 added `videos.duration_seconds` (`20260725120000_video_metadata.sql:28`) — the exact minute count Whisper is billed on. `inline` → 1 credit; `job` → `2 × ceil(duration_seconds / 60)`. Both inputs are already persisted, so a **retrospective estimate over existing rows is possible with zero new plumbing** and is worth running first, before any migration, to size the problem.
    - **Call sites to count** (all in the generate request): `fetchTranscript` — 1 billable call, plus up to 12 *free* `getJobStatus` polls (`src/lib/services/transcript.ts:100-102`, do not count these as spend, but counting them separately is diagnostic — they measure Whisper latency); `fetchVideoMetadata` — 1 call, **plus a second one on the retry path** (`src/lib/services/metadata.ts:146-149`), which is real spend the naive "1 per generation" assumption misses; and the quote-cache hit path, which spends **zero** and must not be counted as one (`getTranscriptQuote`, `src/lib/services/transcript-guard.ts:67`). Threading a small mutable counter through the endpoint is enough — no new abstraction.
    - **Free ground-truth check:** `GET /v1/me` returns `usedCredits` / `maxCredits` and is free to call (`docs/supadata-account-limits.md:9`). Measured 2026-07-25: `maxCredits: 100`, `usedCredits: 29`. Useful for calibrating the derived figure — and note S-09's lever C wants the same endpoint, so if both slices land, the reader belongs in one shared helper. **Settled 2026-07-31:** S-07 shipped and lever C was chosen, so both do land — S-09 owns the reader, and it is not a bare call but a lazily-refreshed stored reading plus a ledger delta (§S-09 D5), because `/v1/me` issued inline would break the 1 req/s spacing before the transcript fetch.
    - **What this buys beyond bookkeeping:** it turns S-09 from a guess into a decision. S-09 must pick between four levers largely on *how often* `mode: "auto"` silently falls back to Whisper and on how long those videos are — a fallback rate nobody has measured. It also makes the S-07/S-09 pair honest: S-07 measures the spend, S-09 bounds it. Sequencing is deliberate — measuring first is cheap and reversible, and the price table warns the plan/throughput figures are unreliable (`docs/README.md`), so an evidence-first order is the safer one.
- **Status:** **done — all 5 phases live in production 2026-07-30.** Rollout was `supabase db push --linked` immediately followed by `wrangler deploy` (Worker version `d6fd806d-4029-4f31-ba13-7ea2d7cfaba7`), closing the `persist_summary` swap window; both migrations applied and `migration list --linked` is in sync. Live verification cost **3 Supadata credits** against the 2–3 budget and is recorded in `context/changes/persist-time-and-cost/reviews/manual-verification.md`. **The slice's core claim held at the vendor's billing level**: a second generation of the same video moved `usedCredits` by exactly 1 — the transcript came from the shared cache for free, and only the metadata call was paid. Reconciliation closed exactly (`sum(billable_credits)` 3 = measured delta 3, zero `unavailable` rows). `cost_usd` proved **exactly** reproducible from token counts at Sonnet 5's introductory rate, to the last digit on both runs — a stronger result than the "plausible" the criterion asked for, though the criterion's stated "~1–2 ¢" range is stale (these runs summarized 16 257 characters and cost 3.2 ¢ / 3.8 ¢). Three findings handed to S-09 — see its Backlog Handoff row. Earlier planning record: 5-phase plan written 2026-07-28 on branch `persist-time-and-cost` (`context/changes/persist-time-and-cost/plan.md`, brief at `plan-brief.md`). Planning settled the open unknowns above and **widened T3 beyond what these scope notes describe**. (1) **Split timings + total**, not a single `generation_ms` — the F-02 spread that motivated F4 was entirely a transcript-branch difference. (2) **Token counts persisted alongside `cost_usd`**, so rows stay comparable across a price change or model swap. (3) **T4 records calls, not credits** — no derived credit figure is stored, because `resolved_via` is documented as a fetch-mechanism observation and *not* a native-vs-Whisper claim (`src/types.ts:3`), so a persisted number would outlive the caveat explaining it; credits are derived at query time. (4) **Spend goes to an append-only `supadata_calls` ledger, not columns on `summaries`** — the open question above is resolved toward the ledger, because columns are structurally blind to a `422 transcript-unavailable`, which costs a real billable credit and writes no summary; `user_id`/`summary_id` are `on delete set null` so the operator's bill survives an account or summary deletion. (5) **T3 changed shape: the transcript body goes to a user-agnostic `transcript_cache` keyed by `youtube_id`, NOT to `videos`.** `videos` is per-user with `on delete cascade`, so one account deletion would erase a transcript others depend on; the shared table also dedupes and holds no personal data. Input size is kept permanently as `transcript_chars` on `summaries`, because the cache row is overwritten on refresh. (6) **Reuse-on-regeneration is IN**, cross-user, with a fixed 30-day window as the only refresh mechanism — no user control, no API parameter, and nothing is ever deleted from the cache. Reuse is marked by widening `resolved_via` to admit `'stored'`, which also measures the cache hit rate. (7) **`persist_summary` is dropped and recreated at 23 params**, knowingly re-opening S-08's push/deploy window. Verification is budgeted: a zero-cost retrospective SQL estimate over existing rows first, then a ~2–3 credit live pass; the Whisper `job` path stays unverified live by design.

### S-08: Persist video metadata

- **Outcome:** the user sees the video's thumbnail, title, YouTube channel name, length, upload date and language alongside a summary, instead of a bare URL — so a saved summary is recognisable at a glance and the watch/skip decision has the context (how long, how old, what language) that the summary text alone doesn't carry.
- **Change ID:** persist-video-metadata
- **PRD refs:** — (no direct PRD ref; makes FR-006's summary list legible)
- **Prerequisites:** S-01
- **Unlocks:** S-02 (the list is the main surface that renders this metadata)
- **Parallel with:** S-07
- **Integrates with:** S-06 (styling of the resulting card)
- **Blockers:** —
- **Unknowns:**
  - Which metadata source? — Owner: implementation. Block: no. Narrowed by a docs check on 2026-07-25: **Supadata's `/metadata` endpoint covers all of it in one call** (`title`, `author.displayName`, `media.duration` in seconds, `media.thumbnailUrl`, `createdAt`) and reuses the key the app already has, so no new provider or quota. YouTube oEmbed is out — it returns title/`author_name`/thumbnail but **neither duration nor upload date**; the YouTube Data API would cover them (`contentDetails.duration`, `snippet.publishedAt`) but needs a new key + quota. The deprecated `GET /youtube/video` should not be used — prefer the unified `/metadata` endpoint.
  - ~~Which "language" do we store — the transcript language or the video's original audio language?~~ **RESOLVED.** Only the transcript language is available, and the columns are named `transcript_lang` / `transcript_available_langs` to say so — a bare `language` column would have been false exactly when a translated track is selected. Supadata's metadata response having **no language field** was assumed on 2026-07-25 and empirically confirmed on 2026-07-27 (entire payload scanned; only `channelId` in `additionalData`), so the video's spoken language would need the YouTube Data API's `snippet.defaultAudioLanguage` — out of scope. The columns are diagnostic and never rendered.
  - ~~**Input-quality constraint — decided 2026-07-25 (user): never trade the original transcript for a machine-translated one.**~~ **RESOLVED 2026-07-27 by the probe this bullet asked for.** The original reasoning (kept in `plan.md` and `change.md`) held that `lang: "pl"` was the setting that could hand the model a machine-translated track, and that the safe move was to drop it. The probe inverted that. `lang` *selects* among existing tracks and never requests a translation — Supadata translates only via a separate, explicitly-called endpoint — so `lang` was never the risk. And omitting it is actively worse: the vendor returns the **first track in the pool**, and pool order carries no information about which track is the original. Two of three videos came back translated, and the German wording reached a delivered summary. `/v1/metadata` carries no language field either, so nothing in the vendor's surface identifies the original track. **Outcome:** the app requests `lang: "en"` and persists `transcript_lang` + `transcript_available_langs`. Residual risk — a Polish-original video that also carries an English track — is measured by those columns, not prevented. Full evidence in `context/changes/persist-video-metadata/docs/supadata-transcript.md` §Language selection.
  - Hotlink the YouTube thumbnail CDN URL or copy the image into storage? — Owner: implementation. Block: no. (Hotlinking is the cheap default for the MVP.)
  - Duration unit and rendering — store raw seconds and format in the UI, or store an interval? — Owner: implementation. Block: no. (Raw seconds matches the API and keeps sorting/filtering trivial.)
- **Risk:** Low risk. `videos.title` and `videos.thumbnail_url` already exist from F-01 but are never written (`VideoInsert`, `src/lib/services/summaries.ts:14-18`), so part of the slice is populating dead columns; the four new fields (channel, duration, upload date, language) need one additive migration. The care point is failure isolation — metadata is decorative, so a fetch failure must not fail a paid generation that already succeeded; persist nulls and render a fallback. Note this adds a **second Supadata call** per generation alongside the transcript fetch — cheap, but it must be non-fatal and should not be retried aggressively. Sequenced after S-01 to avoid contending with the deploy-gated Phase 8 migration, and made a hard prerequisite of S-02 so the list ships with metadata rather than being retrofitted.
- **Scope notes:**
  - **API reference:** captured in this slice's own change folder — `context/changes/persist-video-metadata/docs/` (fetched 2026-07-25; see its `README.md`). `supadata-metadata.md` carries the response schema and the field→column mapping below; `supadata-transcript.md` carries the `lang` caveat.
  - Additive nullable columns on `videos`, one idempotent migration (same pattern as `model` / `resolved_via`): `channel_name text`, `duration_seconds integer`, `published_at timestamptz`, and — as shipped — `transcript_lang text` + `transcript_available_langs text[]` rather than a single `language text`. ~~RLS unchanged~~: the impl-review tightened it instead (F3) — `20260726120000_videos_single_writer.sql` revokes authenticated `insert`/`update` on `videos`, leaving `select`/`delete`, so the service-role `persist_summary` RPC is the only writer of vendor-reported metadata. `videos.user_id`'s `on delete cascade` still covers S-04 erasure.
  - Populate the existing dead `title` / `thumbnail_url` on the same insert path.
  - Metadata fetch happens in the generation endpoint next to the transcript fetch, best-effort and non-fatal; the language fields come free from the transcript result already in hand rather than a separate call. As shipped they are also carried through the `allowLong` quote cache (impl-review F10, `20260727120000_transcript_quote_langs.sql`) — without that they were null on exactly the long-video path, biasing the diagnostics against the longest content.
  - **Adjacency with S-07:** both slices add nullable columns and thread extra fields through the same persistence path (`src/lib/services/summaries.ts`). They target different tables' concerns (S-07: `generation_ms`/`cost_usd`/`supadata_*` on `summaries` + `transcript` on `videos`; S-08: descriptive metadata on `videos`) but will collide on the same insert function if run in parallel worktrees — sequence them or expect a merge.
  - **Supadata cost of the second call (measured 2026-07-25):** `/metadata` is a **flat 1 credit** per request, any platform or media type (docs.supadata.ai/get-metadata §Pricing). Compare with the transcript call: **1 credit** for a native transcript, but **2 credits per minute** for a generated (Whisper) one; job-status polling is free, and a 206 "unavailable" still costs 1 credit. So the second call **doubles** the cost of a cheap generation (native transcript: 1 → 2 credits) and is noise on an expensive one (a 20-min Whisper video is ~40 credits; +1 is ~2%). Live account state via `GET /v1/me`: plan **"Free (100/mo)"**, `maxCredits: 100`, `usedCredits: 29` — so S-08 takes the native-transcript ceiling from ~100 to ~50 generations/month on the current plan. Acceptable at MVP volume, but it is a real halving, not a rounding error.
  - **Rate limit shapes the sequencing:** the free plan allows **1 request/second**. Firing `/metadata` concurrently with the transcript fetch would put two Supadata requests in the same second and earn `limit-exceeded`; because metadata is non-fatal, the failure mode is *silently null metadata* rather than an error — the worst kind of bug for a decorative field. Fetch it **after** `summarize()` returns (seconds past the transcript request, and its own round trip is noise next to the LLM call), then pass it into the persist step. Do not fire it before the cost gates either: the 402/413/409 paths must not spend a credit on a generation that never happens, and the `allowLong` confirmation resubmit would otherwise pay for metadata twice.
  - **The persist path needs a signature change, not just columns:** `videos` is inserted inside the `persist_summary` RPC, which currently takes `p_url`/`p_youtube_id` only and inserts `values (target_user, p_url, p_youtube_id)` (`supabase/migrations/20260723120000_atomic_persist_summary.sql:45-53,113`) — that is *why* `title`/`thumbnail_url` are dead. Writing metadata transactionally means widening that RPC's parameter list. Same function S-07 has to widen, hence the collision noted above.
  - **Pre-existing finding, now tracked as S-09:** at 2 credits/minute for generated transcripts on a 100-credit plan, a single ~50-minute Whisper video would consume the entire monthly Supadata budget, and `fetchTranscript` requests `mode: "auto"`, which falls back to generation silently (`src/lib/services/transcript.ts:30`). Out of scope here; surfaced by this slice's cost check. **Coupling resolved 2026-07-31:** S-09 rejected the duration pre-gate, so `/metadata` does **not** move, stays decorative, and keeps the "fetch after `summarize()`" sequencing and the "persist nulls" failure policy described above. No fail-open/fail-closed decision arises, and there is no ordering constraint between the two slices. What S-09 does add is a **cache lookup in front of** the call (its D6/D7) — the call site itself is untouched.
  - **Cost-policy note:** `duration_seconds` is *not* a substitute for the char-based `LONG_TRANSCRIPT_CHARS` / `HARD_MAX_TRANSCRIPT_CHARS` gates (`src/lib/services/summaries.ts:84-85`). It is display context for the watch/skip decision; no pricing behaviour changes here.
- **Status:** implemented + deployed 2026-07-26, **manual verification still open**. Branch `persist-video-metadata`, four phases: P1 schema + widened RPC (`bd307b2`) — five new columns, `thumbnail_url` → `thumbnail_url_reported` with its rationale comment, `persist_summary` dropped and recreated at 15 params with a coalescing on-conflict clause; P2 transcript language signals (`6553ac0`) — `lang: "pl"` dropped, `availableLangs` surfaced on both the inline and job paths, large-pool `console.warn`; P3 metadata fetch (`17e9730`) — new total `src/lib/services/metadata.ts` (one retry on `limit-exceeded` or a transport rejection, vendor values normalised before typed columns, never throws) wired in after `summarize()` and before the persistence `try`/`catch`; P4 rollout — `supabase db push` and `wrangler deploy` run back to back 2026-07-26 (version `52657520-eac9-47fe-85a4-19f726db4f79`), closing the RPC-swap window. Automated gates green throughout (`supabase migration up`, `npm run lint`, `npm run build`), and the local DB confirms 12 `videos` columns, exactly one `persist_summary` in `pg_proc` and the comment attached. **What is NOT verified: every manual row in the plan** — no real generation has been run against either stack since the change, so the seven-field populate, the thumbnail URL, the metadata-failure and transport-rejection non-fatality, the second-generation coalesce and the `usedCredits` delta are all unconfirmed. The user chose to deploy ahead of that verification. Earlier planning record: 4-phase plan written 2026-07-25 (`context/changes/persist-video-metadata/plan.md`, brief at `plan-brief.md`). Planning settled six things the scope notes left open. **(1) Language is diagnostic, not display.** The columns are named `transcript_lang` / `transcript_available_langs`, never `language`, and nothing renders them — a card labelled "language" would be false exactly when auto-translated tracks exist, and the honest field (the video's spoken language) needs the Data API this slice rejected. **(2) No probe.** The `lang: "pl"` request is dropped outright — it is the one setting that can ask for a machine-translated track — and `availableLangs` is persisted so the open auto-translation question is answered by real traffic instead of a paid one-off; a `console.warn` fires when the pool is large enough to imply auto-translation (threshold a first guess, native sets run 1–3 tracks vs 100+). Note this is a behaviour change on the paid transcript path, with a narrow regression: a Polish video carrying extra human tracks may now get the wrong one, since "first available" has no documented ordering. **(3) Zero UI** — S-02 owns rendering and must supply the null fallback, which it needs regardless because a failed metadata fetch produces the same shape. **(4) The RPC swap is drop + create in one migration, not expand/contract** (user decision): it avoids a window with two ambiguous `persist_summary` overloads, at the cost of requiring `db push` and `wrangler deploy` back to back — in between, the live Worker 500s and refunds, losing the OpenRouter spend but never charging the user. **(5) One targeted retry** on the metadata call (network errors and `limit-exceeded` only; `not-found`/`invalid-request` are permanent), then null. **(6) No backfill** — each row would cost a credit from the 71 remaining. **Gap found during planning, not in these scope notes:** the `allowLong` confirmation path has no transcript in hand (`transcript_quotes` stores content + `resolved_via` only), so both language columns are null there; accepted, `save_transcript_quote` is deliberately not widened. Two hazards captured in the plan's Critical Implementation Details: the upsert must `coalesce` new columns or a second generation with failed metadata erases the first run's, and vendor values must be normalised before hitting `timestamptz`/`integer` or a malformed `createdAt` aborts persist and refunds an already-paid summary.

### S-09: Bound the Supadata cost of one generation

- **Outcome:** (guardrail) the operator's Supadata spend per generation is bounded and predictable — no single video can silently consume the whole monthly credit budget — and when a video cannot be summarised cheaply the user is told so instead of the cost being absorbed invisibly.
- **Change ID:** transcript-cost-guardrail
- **PRD refs:** — (no direct PRD ref; roadmap-originated cost guardrail, the Supadata-side sibling of S-05's OpenRouter-side guardrail)
- **Prerequisites:** S-01 (the generation endpoint is where the guard lives). The former "lever B additionally needs S-08 (duration)" clause lapsed with lever B's rejection — but S-07 is now a *de facto* prerequisite instead: D5's breaker reads the `supadata_calls` ledger and D6's cache mirrors `transcript_cache`. Both shipped, so nothing is blocked.
- **Parallel with:** S-02, S-03, S-07
- **Integrates with:** S-08 — but **not** in the way this line used to say. Lever B's relocation of the `/metadata` call is off the table (rejected 2026-07-31); the real coupling is now D6/D7, which cache what that call returns and leave the call itself untouched. Also S-07, whose ledger and cache pattern this slice builds directly on.
- **Blockers:** —
- **Unknowns:** — none blocking. All three former unknowns were settled by the 2026-07-31 decision session below; the two that only existed under lever B (pre-gate fail-open/fail-closed, duration threshold) lapsed with the lever rather than being answered.
- **Decisions (locked 2026-07-31, user):** the shape of this slice is settled — it is `/10x-plan` input, not an open design question. Numbered as put to the user; D2/D3/D6–D12 were confirmed as recommended, D4 was overridden, D5 was reshaped twice.
  - **Levers: A (`mode: "native"`) + C (org budget breaker, minimal). B and D are rejected.** A bounds the single video, C bounds the fleet — they answer different failure modes and A alone leaves the second open. B is rejected because its whole value was keeping Whisper reachable, which the evidence below undercuts; D is rejected because it burns a credit on the failed native attempt to offer a path A has already declined.
  - **D1 — the "is the job path reachable?" question is closed permanently, deliberately.** Switching to `native` means `resolved_via = 'job'` can never appear again, so S-07's hand-over item (watch the ledger for a `job` row) becomes unanswerable. Accepted knowingly: the exposure is live now, the lever is one word, and the answer would only inform a path the app has decided not to take. **Consequence worth stating plainly: lever A may save nothing measurable.** S-07 could not force the Whisper path at all — five submits across three videos returned `206 transcript-unavailable` under BOTH `auto` and `generate`, never a `202`. If the job path is unreachable on this plan, `mode: "auto"` never actually cost 2 credits/minute and A buys a **contractual** guarantee (worst case known in advance), not a measured saving. That is still worth having; it is not what the slice was originally sold as.
  - **D2 — `mode` stays a hard-coded constant** in `src/lib/services/transcript.ts:255`, no env var. A secret-driven toggle means the expensive path can be re-enabled without a trace in the code, and needs a Worker secret round trip to change either way.
  - **D3 — the `unavailable` 422 gets its own user-facing copy.** Today three distinct causes share one string (`generate.ts:345,401,450`). Under native-only, "this video has no captions" stops being a rare accident and becomes the predictable answer for a whole class of videos, which is exactly the half of this slice's outcome about telling the user instead of absorbing the cost silently. The other two causes keep the current generic string.
  - **D4 — the negative-cache window drops from 24 h to 2 h** (`TRANSCRIPT_CACHE_UNAVAILABLE_MAX_AGE_SECONDS`, `transcript-cache.ts:37`). User override of the recommendation to keep 24 h, on the grounds that a day is too long to wait for a newly published video. The trade is explicit and runs the other way from the rest of the slice: it buys user-visible correctness with operator credits — a caption-less video resubmitted five times in a day now costs 5 credits instead of 1. **The constant's comment must be rewritten, not just its value:** it currently justifies 24 h, and its premise (`unavailable` may mean "Whisper failed") is itself void under native-only, where the outcome means "no caption track exists". This decision is also a live argument for C — see the counterargument note there.
  - **D5 — C ships in minimal form, and its notification is a seam, not a channel.** Three reshapes, in order: (1) the breaker gates the **spend, not the request** — the budget check sits on the miss path, immediately before the first paid call, never at the endpoint entrance, because a cache hit costs 0 and blocking it would break the product for no saving. This defuses the original objection recorded below that C "protects the budget by breaking the product for everyone". (2) The budget figure comes from **`GET /v1/me` refreshed lazily** (stored row, TTL in minutes, refreshed in-line by the request that needs it) **plus a ledger delta since that reading** — neither source suffices alone: `/v1/me` is authoritative but is an HTTP call that would land directly before the transcript fetch and break the 1 req/s spacing `generate.ts:552-556` maintains, while `supadata_calls` is free and instant but does not know when the vendor's billing period resets, so a purely ledger-derived total drifts out of phase. No cron, no scheduler. (3) **The warning is emitted through one named function** (e.g. `reportBudgetThreshold(...)`) that currently only logs. Not cosmetic: error-monitoring tools capture exceptions and treat console output as breadcrumbs attached to *other* events, so a bare `console.warn` may never become an alert. One swappable call site, not a logging abstraction.
  - **D5a — two thresholds, two cadences, and the split is what removes the need for dedup state.** **Stop** is evaluated on **every** miss-path request with the ledger delta included (it is the protection; it must be exact to the call). **Warn** fires only when the `/v1/me` reading is **refreshed**, i.e. at most once per TTL — which rate-limits it to a few events a day with no `warned_at` column and no period-reset logic. A naive "fire whenever above threshold" would be one issue with thousands of events and a burnt free tier. **The stop threshold also emits**, at higher severity: a refusal is an incident, not a warning, and reporting only the warn level would surface the near-miss while hiding the actual outage. Payload must be self-sufficient — `used`/`max`, delta since the reading, which threshold, and the reading's age — so the event is legible without a database query.
  - **D5b — delivery is deferred; nothing receives the event yet.** The seam is built now, the monitoring tool (Sentry or equivalent) lands later, tracked by the user outside this roadmap. The rejected alternatives are recorded so they are not re-derived: Workers Logs alone (`observability.enabled`, `wrangler.jsonc`) is pull-only with days-long retention, useless for a once-a-month urgent condition; an operator script (`npm run budget`, following the `grant-credits.mjs` precedent) is cheap because D5's stored reading exists anyway, but still pull; a Discord/Slack webhook is the only variant that finds you, and was dropped along with its Worker secret when the user chose the monitoring route. **Until a receiver exists the warn threshold is decorative** and budget exhaustion surfaces via the stop threshold — i.e. users seeing an error, the worst channel and the one C exists to avoid.
  - **D6 — full `metadata_cache` keyed by `youtube_id`, not the minimal `videos` lookup.** Same reasoning that kept transcripts off `videos` in S-07: `videos` is per-user and cascades on account deletion, so one deleted account would evict data every other user shares. Mirrors `transcript_cache`.
  - **D7 — the lookup moves early, the fetch does not move at all.** `generate.ts:552-556` documents three reasons for the call's placement; a cache **hit** removes the request outright so the 1 req/s reason lapses on its own, but the other two (early exits never spend a credit, an `allowLong` resubmit never pays twice) govern the **miss** path and still hold. So: add a lookup, leave `fetchVideoMetadata` exactly where it is. No pipeline restructuring — which is also why rejecting lever B costs nothing here.
  - **D8 — 30-day window, for symmetry with `TRANSCRIPT_CACHE_MAX_AGE_SECONDS`.** Metadata is not immutable — titles and thumbnails do change, `duration_seconds` and `published_at` effectively do not. A longer window is a decision to be argued on its own merits, not drifted into because it is cheaper.
  - **D9 — no negative caching of metadata failures.** Per S-07's hand-over item (2), a *failed* metadata call is billed **0** credits, so caching the failure saves nothing while persisting nulls for a video whose next attempt would return the full record.
  - **D10 — a cache hit needs an explicit marker.** Today every call writes a `supadata_calls` row; a hit is not a call, so absence of a `metadata` row would be indistinguishable from "row predates S-08". Same problem S-07 solved for transcripts with `resolved_via = 'stored'`, and it needs the same kind of answer — without one, cost-per-generation queries start lying within a week of shipping.
  - **D11 — no backfill of `metadata_cache` from existing `videos` rows.** Mixed provenance and unknown age; the cold-start cost is one credit per video.
  - **D12 — a cache hit still writes `videos`.** S-02 renders the list from `videos`, so the per-user row must be populated on a hit too: the cache feeds the upsert, it does not replace it. Stated explicitly because "hit → skip the write" is the natural regression.
  - **Out of scope, reconfirmed:** what a summary costs the *user* in app credits (S-05's model) and upgrading the Supadata plan. This slice informs both, decides neither.
- **Risk:** Nice-to-have, and deferrable alongside S-03 under the "speed" goal — the exposure is operator-side (a drained Supadata budget, recoverable by a plan upgrade or waiting for the reset), not user-facing data loss or a privacy breach. It is worth a slice rather than a footnote because the mismatch is structural, not a tuning problem: **the app prices generation in characters, after the fetch, capped at 2 user credits (`summaryCost`, `src/lib/services/summaries.ts:84-85`), while Supadata charges in minutes, during the fetch, uncapped.** So a user can spend 2 of their credits on a video that costs the operator 100 Supadata credits — and `HARD_MAX_TRANSCRIPT_CHARS` cannot help, because it is evaluated only *after* the money is spent. Low implementation risk in every variant: lever A is a one-word change, and the endpoint already has the return paths (422/409/413) that a refusal or confirmation would reuse.
- **Scope notes:**
  - **API reference lives in S-08's change folder:** `context/changes/persist-video-metadata/docs/` — `supadata-transcript.md` (the `mode` lever, `lang` semantics, per-operation pricing, error enum), `supadata-account-limits.md` (`GET /v1/me`, the measured account state, rate limits, and a warning that the plan/price table is unreliable), `supadata-metadata.md` (the endpoint S-08 adds). Captured 2026-07-25 via Context7 plus one live `GET /v1/me`; `docs/README.md` indexes them. No separate fetch needed to plan this slice.
  - **Measured basis (2026-07-25):** Supadata charges **1 credit** for a native transcript but **2 credits per minute** for a generated (Whisper) one; job polling is free; a 206 "unavailable" still costs 1 credit. The account is on plan **"Free (100/mo)"** (`GET /v1/me`: `maxCredits: 100`, `usedCredits: 29`), so **one ~50-minute video on the Whisper path exhausts the month**. Nothing in the code bounds this today — `fetchTranscript` passes `mode: "auto"` (`src/lib/services/transcript.ts:30`), whose documented behaviour is "try native, fall back to generate", and the fallback is silent.
  - **Provenance correction:** the 2-credits/minute price was **not** discovered on 2026-07-25 — it was already recorded in the F-02 research docs on 2026-07-05 (`context/changes/transcript-llm-probe/docs/supadata.md:43`, alongside the three `mode` values). What was new on 2026-07-25 is the arithmetic against the 100-credit plan and the observation that no code path bounds it. A correctly captured vendor fact sat unacted-on for three weeks, which is the reusable lesson here (candidate for `/10x-lesson`).
  - **Lever A — CHOSEN 2026-07-31 — `mode: "native"`:** stop generating entirely. Documented as "only fetch existing transcript", so every transcript then costs exactly 1 credit and worst-case spend per generation is knowable in advance. Caption-less videos become `transcript-unavailable`, which the endpoint already turns into a 422 with a user-facing message — no new response path. Cheapest and most predictable; the cost is a real capability loss.
  - **Lever B — REJECTED 2026-07-31** (its value was keeping Whisper reachable, which D1's evidence undercuts; rejecting it also leaves `generate.ts`'s metadata placement untouched) — **duration pre-gate:** fetch `/metadata` (1 credit) *before* the transcript, refuse or require confirmation above N minutes, then fetch with `mode: "auto"`. Keeps Whisper available for short videos and reuses the existing `allowLong` 409 confirmation pattern. Two consequences: it needs S-08's metadata call moved to the front of the pipeline (and hardened — see the coupling note in S-08), and it also *fixes* the 1-request/second concern by ordering the two Supadata calls naturally instead of concurrently.
  - **Lever C — CHOSEN 2026-07-31, minimal form; see D5/D5a/D5b for the shape actually adopted, which differs from this sketch in that it gates the miss path only — and, per the plan review 2026-07-31, is an *atomic reserve/settle* under the singleton budget row rather than the read-then-spend sketched below** (a read-then-spend breaker bounds nothing: ledger rows only flush in `POST.finally`, so a request's own spend is invisible to every concurrent one) — **org-budget circuit breaker:** poll `GET /v1/me` (free) and refuse generation when `usedCredits` approaches `maxCredits`. Orthogonal to A and B: it caps the fleet rather than the single video, and is the only lever that also covers death-by-many-small-videos. Cheap, but it protects the budget by breaking the product for everyone once tripped, so it belongs behind a warning surface.
  - **Lever D — REJECTED 2026-07-31** (burns a credit on the failed native attempt to offer exactly the paid path A declines) — **two-stage native-then-confirm:** try `mode: "native"` first, and on `transcript-unavailable` tell the user the video needs paid transcription and let them opt in to a `mode: "generate"` retry. Most honest UX and needs no metadata call, but burns 1 credit on the failed native attempt (the 206 is billable) and adds a round trip.
  - **Resulting cost envelope (A + D6 metadata cache, per generation):** cold video with captions **2** credits (1 transcript + 1 metadata); cold video without captions **1** (the 422 exits before the metadata call is reached); warm video inside both windows **0**; caption-less retry after the 2 h window **1**. Worst case is therefore bounded at 2 **and known before any work starts** — **corrected 2026-07-31 by the plan review: the ceiling is 3, not 2.** Metadata retries once on a retryable failure and that retry is separately billed, so a cold video with captions costs 2 *typically* and 3 at the ceiling. The 2-credit figures in this bullet are the typical case; every derived number (stop reserve, verification budget) uses 3 — — which is precisely what makes C implementable as a pre-flight check rather than a mid-flight guess: under `mode: "auto"` in-flight spend was unbounded, so a breaker could only trip after the money was gone. At 100 credits/month that envelope still allows only ~50 cold videos across all users, and **that residual is exactly the fleet-level exposure A cannot touch** — the case for C in one line.
  - **Counterargument to C, recorded rather than buried:** C could be deferred, since after A and the caches the worst outcome is an exhausted free plan — recoverable by upgrading or waiting for the reset — and this whole slice is nice-to-have. The reason it was not deferred: **D4 shortened the negative-cache window to 2 h**, which deliberately trades operator credits for user-visible correctness and multiplies the cost of repeated caption-less submissions. Deferring C would leave that trade without a counterweight. If C is ever dropped, revisit D4 in the same breath — the two decisions are load-bearing for each other.
  - **Not in scope:** ~~changing what a summary costs the *user* in app credits (S-05's model)~~ — **this exclusion was removed 2026-08-01 (user), see D13/D14 below** — or upgrading the Supadata plan, which remains a decision this slice should inform, not make.
  - **D13 — `supadata_calls.http_status`, added 2026-08-01 (user).** Nullable integer, populated for `operation = 'transcript'` only, **no backfill** (an unobserved value is not reconstructed from an inference — same reasoning as D11). Motivated by Phase 1's verification: the ledger records no HTTP status, so "a `206` is billed 1 credit despite sending no `x-billable-requests` header" was a *derived* claim, inferred from `outcome = 'unavailable'` — our own label applied at the call site — while the whole per-outcome reconciliation formula rests on it. Metadata calls are deliberately excluded: their null-billable case is a *failed* call billed 0, informative but not what this closes, and touching `metadata.ts` would pull the retry helper into an otherwise risk-free change.
  - **D14 — an unusable submission costs the user 1 app credit, added 2026-08-01 (user).** Phase 1's live pass measured the asymmetry: a caption-less video costs the operator 1 Supadata credit and the user 0, because every 422 exits roughly 200 lines upstream of `beginGeneration`, the only existing debit. D4's 2 h negative window deliberately multiplies exactly this traffic, and the only counterweights today are a 10-per-10-minute rate limit and a fleet breaker that does not exist yet. **Four of the five 422 exits charge** (cached `unavailable`, cached `empty`, fresh `unavailable`, whitespace); **the fresh transient `failed`/`timeout` exit does not** — that is an outage, ours or the vendor's, and `error` with a null header means the cost is *unknown*, so charging would resolve our own ambiguity against the user. Three consequences recorded rather than discovered later: **(1)** the cache-hit case charges although the operator paid nothing — accepted because the alternative makes one action cost differently depending on cache state the user cannot see, which reads as a bug and rewards rapid resubmission of precisely the videos the 2 h window exists to re-check; **(2)** the charge **ships silently** — copy and 422 body unchanged, the user's explicit call — so a user can lose several credits with no on-screen acknowledgement, and settled `credit_reservations` rows with no `summary_id` are the only signal; **(3)** a revert stops future charges but returns nothing already taken. Implemented as one RPC inserting an already-`settled` row keyed by the request's own `requestId`, reusing the existing unique index for idempotency — a fresh key would bill once per retry, which is the exact case `requestId` exists to absorb.
  - **Interaction with S-07:** two-way. (1) If S-07's transcript persistence lands with reuse-on-regeneration enabled, it independently removes one class of repeat Whisper spend — not a substitute, it helps only on a second generation of the same video, never the first. (2) **S-07's T4 measures exactly what this slice bounds** (added 2026-07-27): per-generation Supadata call count + derived credits, which yields the one number the lever choice below turns on — how often `mode: "auto"` actually falls back to Whisper, and on what video lengths. Landing S-07 first makes A-vs-B an evidence decision instead of a guess, and is cheap and reversible; the counterargument is that the exposure is live *now* and lever A is one word, so an unlucky 50-minute video during the measurement window drains the month. Both slices also want `GET /v1/me` (lever C here, calibration there) — share one reader if both land.
  - **Language side effect worth knowing:** in `generate` mode Supadata **ignores the `lang` parameter** and returns the video's original language, so the app's `lang: "pl"` request is silently dropped on exactly the expensive path. Relevant to S-08's `language` column caveat and to prompt behaviour, not a cost matter. **Moot for the transcript path under lever A** — the app no longer requests `generate` mode — but keep it recorded: it is the reason S-08's `transcript_lang` caveat exists, and it would return the moment anyone reconsidered D2.
- **Status:** **done — all 7 phases live in production 2026-08-06.** Rollout was `supabase db push --linked` immediately followed by `wrangler deploy` in one shell command (Worker version `393417a8-dedc-4a02-906e-f09e5c443637`), closing the `persist_summary` swap window; `migration list --linked` is in sync. **Six migrations were pending, not the four criterion 7.1 names** — the plan's own P5 note already recorded the renumbering (`…150000_supadata_budget`, with `…130000_summaries_single_writer` and `…140000_metadata_via_fetch_failed` as P4 follow-ups), but the count was never corrected; only P4's opened a deploy window. Live verification cost **4 Supadata credits** against the ~4-credit budget and is recorded in `context/changes/transcript-cost-guardrail/reviews/manual-verification.md`. **The slice's headline claim held at the vendor's billing level: the repeat generation moved `usedCredits` by exactly 0** — S-07 got a repeat down to 1 credit by caching the transcript, this gets it to 0 by caching the metadata too, and that request produced zero ledger rows *and* zero budget reservations, which is D5's "the breaker gates spend, not the request" observed rather than argued. Reconciliation closed exactly (per-outcome ledger total 4 = measured delta 4), and **the `unavailable → 1` branch was corroborated by the vendor's own `http_status = 206`** rather than by our `outcome` label — D13's whole purpose, now exercised against production billing. Four results worth carrying: the breaker **initialized itself on the first real production request** (seeded-null row → `refresh_required` → `/v1/me` → save → the *second* pass decides, its reservation stamped strictly newer than the reading that authorized it); the **stale-reading design was exercised for real** — `read_at` never moved past the first request because the 900 s TTL had not elapsed, so steps 2–4 decided against a reading already 4 credits stale and were still correct, the outstanding reservations carrying the difference; **step 4 justified the second check point** — a warm transcript with cold metadata made the metadata call the request's *only* paid call, so a single-point breaker would have been bypassed on exactly the traffic P4's cache creates; and **no reservation was stranded** across a success, a warm no-op, a 206 refusal and a metadata-only run, so the sweep never had to act. **The 1-credit refusal charge (D14) is now live for real users** and ships silently by design — watch `credit_reservations` filtered on a non-null `refusal_reason` in the first day; its rate by reason is the first evidence of whether the friction is proportionate. **What stays unverified in production, by choice:** the stop threshold (reaching it honestly costs ~95 credits — P6 proved it locally under an overridden constant), the fail-open paths (they need a broken vendor endpoint), the warn threshold's delivery (D5b — no receiver yet), and the Whisper job path, which under D1 is **unreachable by construction and will never be verified at all**, leaving lever A's saving contractual rather than measured. Implementation record follows. **P6's manual rows 6.8–6.17 closed 2026-08-06** (record `reviews/manual-verification-phase-6.md`, commit `f23ce3b`) — 3 Supadata credits, reconciled exactly, and **seven of eleven submissions cost nothing**, because a breaker is mostly verifiable on paths where no vendor call happens. Four results worth keeping: a clean deployment **initializes its breaker on its first real request** (uninitialized row → `refresh_required` → `/v1/me` → save → the *second* pass decides), the app-level counterpart of P5's SQL row 5.9; a **malformed `/v1/me` never reaches the anchor** (`max_credits` still null afterwards — destructured on faith it would go `NaN` and the breaker would silently never trip again); the **metadata retry fired unprompted**, so `billedSince` met a real mixed batch and settled `null` rather than 1, the first live sighting of the separately-billed retry `METADATA_BUDGET_CREDITS = 2` exists for; and **two concurrent stale readers produced exactly one warn and one refresh**, proved by a 6× wall-time gap (1.68 s vs 0.26 s) rather than by counting log lines. **P5's manual rows 5.13–5.14 closed 2026-08-05**, re-run against the revised migration after the impl-review triage — 5.14's earlier walk used a `save_supadata_budget` signature that no longer exists. **Phases 4, 5 and 6 landed 2026-08-04.** P4 (`metadata_cache` + the `metadata_via` marker, `0142ead`, review triaged in `e6473e8`) is manually verified — **the warm generation moved `usedCredits` by 0**, the slice's headline claim, observed locally for the first time, and the pass reconciled exactly (delta 4, ledger total 4). Its one lasting correction: `metadata_via` gained a fourth value, `'fetch_failed'`, because a single `'fetched'` covered both "the call returned metadata" and "the call returned nothing" while every comment on it read *billed* — and a failed metadata call is billed 0, which is exactly why D9 does not cache it. P5 (the reservation ledger, `00353e2`) is a migration and nothing else, and the two properties that cannot be established by reading code were proved in **two concurrent psql sessions**: one seat yields one id and one refusal, and exactly one of two stale readers takes the refresh claim — in both runs the second session *blocked* on the singleton row lock until the first committed, which is what makes them serialization proofs rather than coincidences (record: `reviews/sql-assertions-phase-5.md`). Two adaptations there: the migration is `20260731150000_supadata_budget.sql`, not the plan's `130000` (that slot went to a P4 follow-up), and the **refresh claim's TTL was a gap the plan left open** — it needs one, since a failed refresh leaves its claim to expire, but it cannot become a fifth argument without breaking the signature P6 mirrors, so it is a derived 30 s constant inside the function. P6 (`01b0f34`) wires the breaker at **two** check points, both on a miss path: before the transcript fetch (reserve 1, and deliberately *ahead of* `recordTranscriptAttempt` so budget refusals cannot burn a user's transcript allowance) and before `fetchVideoMetadata` (reserve **2** — the retry is separately billed). A trip at the first returns 503; a trip at the second does **not** refuse the generation, since the user is already debited and the LLM already paid for — it persists nulls and records `metadata_via = 'skipped_budget'`. `reserveBudget` resolves to a **three-case** union whose third case, `untracked`, is the fail-open: proceeding *without* a reservation is neither `reserved` nor `refused`, and collapsing it would make "we could not track this call" indistinguishable from "we tracked it" at the two sites that decide whether to settle. **P5/P6 manual rows (5.13–5.14, 6.8–6.17) remain open** — they need a running app and a temporarily raised `BUDGET_STOP_RESERVE`, and 6.17 is reverting it before P7. **Phases 2 and 3 closed 2026-08-04.** P2 (`supadata_calls.http_status`, D13) landed as `a5efa28`, impl-review APPROVED; one adaptation was approved during implementation — the plan's "no RPC change" sub-clause was wrong, since `record_supadata_calls` enumerates its columns and would have silently dropped the new value, fixed with a `create or replace` on the same `(jsonb)` signature so the phase stays additive. P3 (the 1-credit refusal charge, D14) landed as `6a62cf1`, triaged in `adb6af0`; its impl-review returned NEEDS ATTENTION and all 3 findings were fixed, of which **F1 made `requestId` required in `generateSchema`** — leaving it optional exported the "no key ⇒ skip the charge" rule to the trust boundary, letting any authenticated caller drive the paid `unavailable` path for free. **Manual rows 2.6–2.9 and 3.9–3.15 all pass** (record: `reviews/manual-verification-phases-2-3.md`, run as one pass because the phases share submissions). Supadata delta 3 reconciled exactly against the per-outcome ledger total — and **for the first time the `unavailable → 1` branch was corroborated by the recorded `http_status = 206`** rather than resting on our own `outcome` label, which is exactly what D13 exists for; the `error` row carried `401` and correctly scored 0. Two things worth carrying: row 3.11 (a repeated `requestId` replays the 422) is only meaningful paired with 3.12 (an operator-settle key still answers 409), because the response alone cannot distinguish a replay from a re-run of the cached-`unavailable` path; and `z.uuid()` rejects a hand-typed non-v4 key with a 400, reachable only now that F1 made the field required. Next is `/10x-implement transcript-cost-guardrail phase 4`. Phase 1 record follows. Phase 1 (lever A `mode: "native"` + the `unavailable` 422 split) landed on branch `transcript-cost-guardrail` (`1e134df`, Progress SHA written by `f05930d`). Impl-review **APPROVED** — 0 critical, 1 warning, 2 observations, all three triaged and fixed; lint + build green. **Manual rows 1.5–1.8 all pass** (record: `reviews/manual-verification-phase-1.md`): the caption-less 422 carries the new specific copy *and* survives the trip through the client, `'empty'` still gets the generic string (the three-way split at `:345`), the happy path is unaffected, and the 2 h window is proven by backdating rather than waiting. **Supadata delta 3 reconciled exactly** against the per-outcome ledger total — independent re-confirmation that a `null` `billable_credits` on a 206 means 1 credit, not 0. **Phase 1 must not deploy in isolation** — Phase 7 is the single deploy gate, so the shortened 2 h negative-cache window cannot ship ahead of the Phase 6 breaker. **Scope extended 2026-08-01 (user): the plan is now 7 phases.** Phase 1's verification surfaced two gaps, inserted as P2 (`http_status`, D13) and P3 (the 1-credit refusal charge, D14), with the metadata cache, reservation ledger, breaker wiring and deploy shifted to P4–P7. D14 removes this slice's "not changing what a summary costs the user" exclusion; `summaryCost` for a delivered summary stays S-05's. Planning record follows. **Planned 2026-07-31** — plan + brief in `context/changes/transcript-cost-guardrail/`; **the plan was 5 phases, not the 4 originally written** — plan review (2026-07-31) split the breaker into a reservation-ledger phase (migration only, proved in SQL) and an endpoint-wiring phase, pushing the deploy + live pass one step later. Planning added nothing to the locked design and re-opened none of it; it settled the four things D1–D12 deliberately left unpicked, and found one gap in the code the decisions did not anticipate. **(1) D10's hit marker: a `summaries.metadata_via` column** in `('fetched','stored','skipped_budget')`, chosen over a ledger row for literal symmetry with S-07's `resolved_via` — the cost is a third `persist_summary` drop+recreate (23 → 24 params) and its push/deploy window, owned by the plan's Phase 4. **(2) D5's breaker gets TWO check points, not one.** D5 says "immediately before the first paid call", which silently assumes the transcript fetch *is* that call; on a warm transcript with cold metadata it is not — `fetchVideoMetadata` is, and it runs post-debit, post-LLM. A single-point breaker would therefore be bypassed on exactly the traffic D6's cache exists to create. On a trip at the metadata point the generation is **not** refused (the user is already debited and the LLM already paid): the call is skipped, nulls persist as they already do on a metadata failure, and `metadata_via = 'skipped_budget'` records why. **(3) Thresholds: stop at `remaining < 3`** (`BUDGET_STOP_RESERVE = 3`) — **corrected from 2 by the plan review, 2026-07-31**: `fetchVideoMetadata` retries a retryable failure once and that retry is a *separately billed* request, so the per-generation ceiling is 1 transcript + 2 metadata = **3**, not 2. Still derived from lever A's worst-case envelope rather than picked, so it needs no retuning if the plan changes — warn at 80 % used, reading TTL 15 min, and the breaker **fails open** with the unreadable state itself reported through the seam (a `/v1/me` outage must not break a product whose transcript API is fine; this deliberately differs from `recordTranscriptAttempt`'s fail-closed rate limit, which guards unbounded spend rather than a bounded, recoverable overrun). **(4) Verification budget ~4 credits** (≤6 worst case), live, reconciled against `GET /v1/me`; the stop threshold is verified by overriding the constant, since reaching it honestly costs ~95 credits. **The code gap D3 did not anticipate:** `GenerateSummaryForm.tsx:73` hardcodes the 422 copy and *discards* the server's `error` string — unlike 429/500/400, which prefer it — so a new server-side message would never reach a user; the client must join the `serverError`-preferring cases. Also found: `generate.ts:345` answers for **both** `'empty'` and `'unavailable'` cache rows, so D3's split is three-way, not two. Both roadmap caveats are carried into the plan verbatim (lever A may save nothing measurable; A+C do not remove the ~50-cold-videos/month ceiling). Prior status: decided — ready to plan (levers and all twelve decisions locked 2026-07-31; no open unknowns).

### S-10: Screen the transcript for injection before it reaches the LLM

- **Outcome:** (guardrail) a transcript is checked for injection attempts before it is sent to the model, and a transcript that fails the check — or that cannot be checked — is not sent at all. The user is told the video could not be summarized rather than receiving a summary shaped by the video's author instead of by the app.
- **Change ID:** transcript-injection-guardrail
- **PRD refs:** — (no direct PRD ref; roadmap-originated integrity guardrail, sibling of S-05's OpenRouter-side and S-09's Supadata-side cost guards — this one guards the *prompt boundary* rather than spend)
- **Prerequisites:** S-01. **Sequence after S-09** — see below.
- **Parallel with:** S-02, S-03
- **Blockers:** —

**Decisions already made (user, 2026-07-31) — these are settled inputs to planning, not open questions:**

1. **Injection only — the app does NOT screen subject matter.** YouTube already moderates what it hosts, so re-adjudicating the topic of a video would duplicate someone else's moderation with worse information, and would contradict the product's premise: summarizing a video *about* a difficult subject is exactly the watch/skip decision the app exists to serve. What has no legitimate reading is a transcript that tries to *steer the model*. That, and only that, is the target.
2. **Fail-closed.** If the check cannot produce a verdict — screen unavailable, timed out, errored, unparseable — the generation is refused. Unchecked content does not reach the LLM. This is the deliberate choice of the stricter side: availability of the summarization flow yields to the guarantee that nothing unscreened is forwarded.

- **What fail-closed commits the plan to** (consequences to design for, not arguments to revisit):
  - **A real verdict-producing check is now part of the slice.** Fail-closed is a rule about what to do when a check fails, so there has to be a check that can pass or fail. Prompt-level hardening (delimiting the transcript, telling the model the material is data and not instructions) produces no verdict and therefore cannot satisfy this on its own — it is worth doing as defence-in-depth, but it is not the deliverable.
  - **Two different refusals, two different messages.** "We checked this and refused it" and "we could not check it" are not the same event and must not share user-facing copy — the second is an outage the operator needs to see and the user should be invited to retry, the first is not.
  - **A refusal must not cost the user a credit.** The app already has pre-debit exit paths that refuse without spending; the screen belongs on that side of the debit so no refund path is needed. Note the Supadata transcript credit is unavoidably already spent by screening time — that operator-side cost on an outage is the accepted price of fail-closed.
  - **Precision matters more than it would under fail-open**, because a false positive now blocks generation outright. The refusal rate needs to be visible to the operator; a guard nobody can measure cannot be shown to be calibrated. S-07's ledger is the precedent for how this project records that kind of fact.
- **Deliberately not specified here: the mechanism and its placement.** Local heuristics vs. a small screening model vs. something else, where exactly the check sits in the request, whether a verdict is cached per video, and what it costs in latency and money — all deferred to planning. **This deferral rested on S-09 restructuring the pre-LLM pipeline — and that premise is now void (2026-07-31).** S-09 chose lever A over the duration pre-gate, so the metadata call does not move and the pre-debit gate order is unchanged; the "details with a known expiry date" argument no longer applies. **The `plan after S-09` sequencing is left standing anyway, but on a weaker and different footing:** S-09 still adds a breaker and a cache lookup to the same pre-debit stretch, and treating the two as one sequence of pre-LLM gates remains the better design. That is now a preference, not a blocker — revisit if S-10 becomes urgent, since planning them in parallel is no longer unsafe.
- **Risk:** The defect is structural and present today — the transcript is passed to the model in the position the model treats as the user's own request, with the app's instructions carried separately, so caption text competes with app instructions on equal footing. Fail-closed raises the stakes of the implementation relative to a permissive guard: the check becomes a hard dependency of the core paid flow, so its reliability, its timeout budget and its false-positive rate are all load-bearing rather than best-effort. That is the main thing planning has to get right, and the main reason not to reach for a heavyweight mechanism first. Cost and latency are real but secondary — per-summary spend is already measured (S-07), so the overhead of a screen is quantifiable before it ships rather than after.
- **Not in scope:** screening subject matter (decision 1); moderating the *summary output*; user-reporting or appeal flows; per-user trust or rate models.
- **Status:** proposed

## Backlog Handoff

| Roadmap ID | Change ID                 | Suggested issue title                          | Ready for `/10x-plan` | Notes                                       |
| ---------- | ------------------------- | ---------------------------------------------- | --------------------- | ------------------------------------------- |
| F-01       | video-summary-schema      | Data schema: videos/summaries tables + RLS     | done                  | Implemented + impl-reviewed (`impl_reviewed`); pending `/10x-archive`. |
| F-02       | transcript-llm-probe      | Spike: YouTube transcript → LLM on the Worker  | done                  | De-risk complete — free CF plan sufficient, Railway not needed. Implemented + impl-reviewed (`impl_reviewed`); pending `/10x-archive`. |
| S-01       | generate-and-save-summary | Generate and save a video summary              | done                  | Plan written 2026-07-18 (now 8 phases — former `S-01-fu` folded in as Phase 8, 2026-07-19), plan-reviewed + hardened 2026-07-18→19 (RETHINK → SOUND; all 7 findings fixed). **Implementation 2026-07-19 (branch `generate-and-save-summary`, Linear MAR-7 → In Progress): phases 1–7 of 8 done** — P1 English copy (`4f48536`), P2 `spend_credits`/`refund_credits` (`23d59cc`), P3 cost policy (`b22d6f1`), P4 endpoint rename & harden (`9072baa`), P5 `allowLong` 409 gate (`17817f2`), P6 dashboard island (`10b3ffa`), P7 prompts + Markdown rendering (`af3bea3`); automated checks green, P7 manual pass passed. **Shipped to production 2026-07-24**: phases 1–7 merged to `master`, Worker deployed, 9 expand migrations pushed to prod Supabase, manual e2e acceptance passed. **Phase 8 implemented + committed 2026-07-24** (`9085d03`): `20260724120000_drop_legacy_rpcs.sql` drops all six superseded RPCs (widened from `spend_credit()` alone to also cover `spend_credits`/`refund_credits`/`reserve_credits` + the two `generation_lock` fns) and removes the dead `reserveCredits` wrapper; automated gates 8.1–8.4 + local-DB verification green. **Phase 8 shipped to prod 2026-07-24**: merged to `master` (`f415f12..c0469d5`), Worker redeployed (CI 30128397834), `supabase db push` applied the drop, migrations in sync. Cloud checks 8.5–8.7 closed — deployed Worker had no call site for any dropped RPC, a prod `db dump` shows all six absent, `settle_reservation`/`reconcile_reservation` retained `service_role`-only; the live generation smoke test was accepted without re-execution (covered by the 2026-07-23 manual e2e). Impl-review APPROVED, triage complete. Linear MAR-7 → Done. Pending `/10x-archive`. |
| S-02       | browse-summary-list       | List of saved summaries                        | planned               | **Planned 2026-08-07** — 4-phase plan + brief in `context/changes/browse-summary-list/`, branch `browse-summary-list`; ready for `/10x-implement browse-summary-list phase 1`. No migration and no change to the paid path. Phases 1–2 (read path, then list UI) are additive and independently shippable — after Phase 2, FR-006 is met. Phase 3 lifts the generation lifecycle out of `GenerateSummaryForm` into a hook so a dismissable dialog cannot orphan an in-flight paid request; it carries no new behaviour and is where the regression risk sits. Phase 4 adds in-progress cards. Adds `GET /api/summaries`, which **S-03 should reuse** rather than building its own read. This slice does NOT restyle anything — S-06 still owns cross-surface coherence — but note it widens `/dashboard` past the `max-w-lg` card and introduces a dialog, so S-06 inherits a different starting layout than the one described in its scope notes. Historical record: **Unblocked 2026-07-26** — both prerequisites are met: S-01 shipped, and S-08 is implemented and deployed (manual verification still open, but the schema and write path are live, so nothing S-02 renders can change shape). Read the new columns as `thumbnail_url_reported`, `channel_name`, `duration_seconds`, `published_at` — note the thumbnail rename, `videos.thumbnail_url` no longer exists. S-08 ships **no UI**, so S-02 owns all rendering — including a null-metadata fallback, which it needs regardless since existing rows are not backfilled and a failed metadata fetch produces the same shape. Language is stored as `transcript_lang` (the transcript's language, not the video's) and is deliberately **not** for display. |
| S-03       | delete-summary            | Delete a summary                               | no                    | Waiting on S-01; FR-007 nice-to-have         |
| S-04       | delete-account            | Delete account + all data (GDPR)               | done                  | Merged to `master` 2026-07-13 (`78bca68`), Linear MAR-10 → Done. Both phases implemented; Workers Secret set. Manual E2E passed; impl-review NEEDS ATTENTION (2 warnings, 3 observations), triage complete (F1/F3/F4 fixed, F2 skipped baseline drift, F5 tracker sync); lint + build green. Pending `/10x-archive`. |
| S-05       | summary-credits           | Credit budget for summary generation           | done                  | Merged to `master` 2026-07-14 (`482b686`), CI green + Worker deployed, Linear MAR-11 → Done. Formal review: **NEEDS ATTENTION** (5 warnings + 3 observations), 2026-07-13; **all 8 findings triaged + fixed 2026-07-14** (`10f91f4`), every dimension now PASS. All 4 phases implemented + manually verified. All 6 DB migrations are **live on cloud** (2026-07-14), privilege state verified post-push. Pending `/10x-archive`. |
| S-06       | app-design-system         | Production-like application design             | no                    | Waiting on S-02 (needs the surfaces it styles to exist); presentational scope only |
| S-07       | persist-time-and-cost | Persist summary generation time + provider cost | **done** | **Done 2026-07-30 — live in production, no longer a backlog item.** All 5 phases landed on branch `persist-time-and-cost`; live verification record at `reviews/manual-verification.md`. Cross-user transcript reuse confirmed against Supadata's own billing (repeat generation of the same video = 1 credit, not 2). **What S-09 gains:** the `supadata_calls` ledger is live and recording, so the Whisper-fallback rate it needs is now accumulating from real traffic at zero extra cost. Original planning record follows. **Planned 2026-07-28** — 5-phase plan + brief in `context/changes/persist-time-and-cost/`, branch of the same name; change-id renamed from `summary-generation-telemetry`. Scope as planned differs from the notes below in five ways: split timings + total (not one `generation_ms`); tokens persisted with `cost_usd`; Supadata spend recorded as an **append-only `supadata_calls` ledger of calls** with credits derived at query time (no stored credit figure — `resolved_via` is not a Whisper claim, `src/types.ts:3`); the transcript body moved out of `videos` into a **user-agnostic `transcript_cache`** keyed by `youtube_id` (per-user `videos` would cascade-delete a shared transcript on one account deletion), with `transcript_chars` kept on `summaries`; and **cross-user reuse-on-regeneration is in**, on a fixed 30-day window with no user control, marked by a new `resolved_via = 'stored'`. Historical framing: carries F-02 impl-review F4 (`generation_ms`) + F5 (`cost_usd` via OpenRouter usage accounting) — **deferred to S-01 but missed during S-01 planning**, found 2026-07-20. Small and well-specified: one additive migration + two service touches; feasibility already confirmed. Waits on S-01 landing (incl. deploy-gated Phase 8) to avoid contending on the same files. **Scope widened 2026-07-27** with T4: also record **Supadata** spend per generation (API call count + credits derived from the vendor price table — the response carries no cost field). Adds one endpoint touch on top of the service touches. Two decisions for the user, neither blocking: per-summary columns vs. a per-call ledger (columns miss billable failures — a 422 costs 1 credit), and derived credits vs. the free `GET /v1/me` counter. Cheap first step needing **no migration**: estimate retrospectively over existing rows from `summaries.resolved_via` + `videos.duration_seconds`. Feeds S-09 — it supplies the unmeasured Whisper-fallback rate that slice's lever choice depends on. |
| S-08       | persist-video-metadata    | Persist video metadata (thumbnail, title, channel, length, upload date, language) | done                  | **Implemented + deployed 2026-07-26** on branch `persist-video-metadata` (P1 `bd307b2`, P2 `6553ac0`, P3 `17e9730`, P4 `db push` + `wrangler deploy` back to back). Automated gates green. **Verification closed — this row previously said otherwise and was stale, corrected 2026-07-31.** It was written on 2026-07-26 *before* the manual run, claiming every row still open and no real generation since the change; both statements were already false by the end of that day and stayed uncorrected for five days, misinforming S-09's planning input. Actual state: all 18 manual rows executed 2026-07-26, rows 2.3/2.5 re-run and 2.6 struck after the F9/F11 fixes on 2026-07-27, **Progress 27/27**, impl-review **APPROVED** (11 findings, none skipped), `change.md` `status: impl_reviewed`. Pending `/10x-archive` only. Unblocks S-02. Historical record: gap found 2026-07-20: F-01 created `videos.title` / `videos.thumbnail_url` but no code ever writes them; no column exists for channel, length, upload date or language. **Scope widened + renamed 2026-07-25** to add length, upload date and language. Source decision effectively settled by a docs check the same day: Supadata `/metadata` returns title, `author.displayName`, `media.duration`, `media.thumbnailUrl` and `createdAt` in one call on the existing key (oEmbed lacks duration/upload date; Data API would need a new key + quota), and language comes free from the transcript response's `lang` — with the caveat that it is the *transcript's* language, not necessarily the video's. Needs one additive migration: `channel_name`, `duration_seconds`, `published_at`, `language`. **Hard prerequisite of S-02** so the list ships with metadata rather than being retrofitted. S-01 has since shipped, so this is unblocked. **Planned 2026-07-25** — 4-phase plan + brief in the change folder; language rescoped to two diagnostic columns (`transcript_lang`, `transcript_available_langs`) with no UI, `lang: "pl"` dropped from the transcript request, `persist_summary` widened via drop+create in one migration. Next: run the plan's manual verification, then `/10x-impl-review` and `/10x-archive`. |
| S-09       | transcript-cost-guardrail | Bound the Supadata cost of one generation      | **done** | **DONE 2026-08-06 — all 7 phases live in production, no longer a backlog item.** Deployed as `db push --linked` + `wrangler deploy` back to back (Worker `393417a8-dedc-4a02-906e-f09e5c443637`, six migrations); live pass recorded in `reviews/manual-verification.md`. **4 credits spent against a ~4-credit budget, reconciled exactly (per-outcome ledger total 4 = delta 4), and the repeat generation moved `usedCredits` by 0** — the slice's headline claim confirmed at the vendor's billing level. The `unavailable → 1` branch was corroborated by the recorded `http_status = 206` rather than by our own label. **Unblocks S-10.** Pending `/10x-archive` only. Implementation record follows. **Phases 1–6 were COMPLETE and all six manually verified, all local and undeployed (P5 rows closed 2026-08-05, P6 rows 2026-08-06 in `f23ce3b`; record `reviews/manual-verification-phase-6.md` — 3 credits, reconciled exactly, and the breaker's fail-open, `skipped_budget`, once-per-TTL warn and settle-in-`finally` behaviours all observed rather than reasoned about). P4 `0142ead` (manually verified — the warm generation moved `usedCredits` by **0**, and `metadata_via` gained `'fetch_failed'` so the marker stops conflating "returned nothing" with "billed"), P5 `00353e2` (the reservation ledger; the concurrent-reserve and single-refresher properties proved in two psql sessions, record `reviews/sql-assertions-phase-5.md`; migration is `…150000_supadata_budget.sql`, and the refresh claim's TTL — a gap the plan left open — is a derived 30 s constant), P6 `01b0f34` (two check points, 503 on a transcript trip, `skipped_budget` on a metadata trip, `reserveBudget` a three-case union whose `untracked` fail-open is a real outcome rather than a null id). **Nothing is open before P7** — the P5/P6 manual rows (5.13–5.14, 6.8–6.17) are closed. Historical detail follows. **Phases 1, 2 and 3: each impl-reviewed with all findings fixed, and manually verified locally (rows 1.5–1.8 on 2026-08-01; rows 2.6–2.9 and 3.9–3.15 on 2026-08-04, record `reviews/manual-verification-phases-2-3.md`). Scope was extended 2026-08-01 to 7 phases — new P2 (`http_status`, D13) and P3 (1-credit refusal charge, D14); old P2–P5 became P4–P7.** **P2/P3 headline:** the reconciliation formula's `unavailable → 1` branch is no longer an inference from our own `outcome` label — the same row now carries the vendor's `http_status = 206`, and the two agree; the `error` row carries `401` and correctly scores 0. The refusal charge behaves as designed across all seven rows, including the deliberate asymmetry (a cache-hit refusal charges although the operator paid nothing) and the exemption (a transient failure does not charge). **P3's F1 is the one to remember**: `requestId` is now **required** in `generateSchema`, because leaving it optional exported the "no key ⇒ skip the charge" rule to the trust boundary. **Next is P4** — `metadata_cache` + the `metadata_via` hit marker, which carries the slice's one risky migration step (the `persist_summary` 23 → 24 argument swap and its push/deploy window). Phase 1 (lever A `mode: "native"` + the `unavailable` 422 split, incl. the client fix so 422 prefers the server string) is committed on branch `transcript-cost-guardrail` (`1e134df`); lint + build green; verification record at `reviews/manual-verification-phase-1.md`, Supadata delta 3 reconciled exactly. **Pre-flight lesson from that pass:** a dev server started three days earlier was still holding port 4321 and serving pre-change code — confirm the port under test is served by a process started *after* the commit under test. **Do not deploy Phase 1 alone** — **P7** is the single deploy gate (this row said P5 until 2026-08-04, a number left stale by the scope extension), so the 2 h negative-cache window must not ship before the P6 breaker. **PLANNED 2026-07-31 — 5-phase plan (not 4; plan review split the breaker into P3 ledger / P4 wiring / P5 deploy) + brief in `context/changes/transcript-cost-guardrail/`; the stop reserve is 3 credits, not 2 — metadata's retry is separately billed. See §S-09 Status for what planning settled and the one code gap D3 did not anticipate (the client discards the server's 422 string).** **DESIGN LOCKED 2026-07-31 — full record in §S-09 Decisions (D1–D12); the lever question below is answered, read it as history.** Levers **A + C**, B and D rejected. A: `mode: "native"` as a hard-coded constant, its own 422 copy for `unavailable`, and the negative-cache window cut **24 h → 2 h** (user override — buys correctness on freshly published videos with operator credits). C: minimal breaker gating the **miss path only**, budget read from a lazily-refreshed `GET /v1/me` row plus a ledger delta, stop evaluated per request and warn only on refresh, both emitted through **one swappable function** whose receiver (Sentry or equivalent) lands later — so the warn level is decorative until then, tracked by the user outside this roadmap. Metadata: **full `metadata_cache` keyed by `youtube_id`** (30-day window, no negative caching, no backfill, explicit hit marker in the ledger, `videos` still written on a hit) with the **lookup** added early and `fetchVideoMetadata` left exactly where it is. Resulting envelope: 2 credits worst case per generation, 0 on a warm video. **Two honest caveats carried into planning:** lever A may save nothing measurable (S-07 never reached the Whisper path at all), buying a contractual bound rather than a proven saving; and A+C together do not remove the ~50-cold-videos/month fleet ceiling on the Free plan. Historical record follows. **Four inputs handed over by S-07, 2026-07-30** (full detail in `context/changes/persist-time-and-cost/reviews/manual-verification.md`). **(1) The `mode` levers must be re-established before anything is planned on them.** S-07 could not force the `job`/Whisper path at all: a caption-less video returns `206 transcript-unavailable` under **both** `mode=auto` and `mode=generate`, and five submit attempts across three videos produced no `202`. Both live runs resolved `inline`, with zero `transcript_poll` rows. This slice's headline lever is switching `mode` — so whether `resolved_via = 'job'` is even a usable Whisper proxy is **open and unbudgeted**, to be answered by watching the live ledger for any `resolved_via = 'job'` or `operation = 'transcript_poll'` row after the first weeks of use. **An empty result is itself the finding** — treat "the job path may be unreachable on this plan" as an open question in either direction, not an assumption. **(2) `billable_credits = null` has two distinct meanings**, both now observed: a `206 transcript-unavailable` is billed 1 credit and carries no header, while a *failed* metadata call is billed 0 and also carries no header. They are indistinguishable from `billable_credits` alone — reconciliation must branch on `outcome`, which is why the formula is per-outcome and not a flat sum. Do not simplify it. **(3) Metadata is now the entire cost of a repeat generation — consider caching it the way S-07 cached transcripts.** `fetchVideoMetadata` is called unconditionally on every successful generation (`generate.ts:568`) with no lookup, even though S-08 already persists `channel_name`/`duration_seconds`/`published_at` to `videos`. Measured: run 2 re-paid a credit for metadata on a video whose `videos` row was already populated by run 1. With the transcript cache landed, that credit is 100 % of a repeat generation's spend. Two variants: **minimal** — check `videos` before fetching, same-user only, no new table; **full** — a `metadata_cache` keyed by `youtube_id`, mirroring `transcript_cache` (right shape for the same reason as transcripts: `videos` is per-user and cascades on account deletion). **Caveat:** `generate.ts:552-556` documents the call's placement as load-bearing for three reasons (1 req/s spacing, early exits not burning a credit, no double-pay on an `allowLong` resubmit) and flags relocation as "S-09 lever B's job, not a free tidy-up". A cache *hit* removes the request outright, so the spacing reason lapses on its own — but this needs deciding, not assuming. **(4) Pricing note, non-critical:** the `cost_usd` figures in S-07's record are at Claude Sonnet 5's **introductory** rate ($2/$10 per MTok, ends 2026-08-31). Budget against the standard $3/$15 — the same two runs cost $0.0569 and $0.0324 there, roughly 1.5×. **API reference already captured** — `context/changes/persist-video-metadata/docs/` (S-08's folder): `supadata-transcript.md` + `supadata-account-limits.md` cover every fact this slice needs, no fetch required. Tracked 2026-07-25 while costing S-08's second Supadata call; the underlying price was already in the F-02 docs since 2026-07-05 (`transcript-llm-probe/docs/supadata.md:43`) — what was new is the arithmetic, not the fact. Supadata bills **2 credits/minute** for generated (Whisper) transcripts vs. 1 for native, and `fetchTranscript` uses `mode: "auto"`, which falls back to generation silently (`src/lib/services/transcript.ts:30`) — so on the current "Free (100/mo)" plan **one ~50-minute video drains the month**. Structural mismatch, not tuning: the app prices in characters *after* the fetch and caps the user at 2 credits, Supadata bills in minutes *during* it, uncapped, so `HARD_MAX_TRANSCRIPT_CHARS` cannot help. Four candidate levers (native-only / duration pre-gate / org circuit breaker / native-then-confirm) — needs a user decision on which, and on whether losing caption-less videos is acceptable. **Nice-to-have**: deferrable alongside S-03, exposure is operator-side. Couples to S-08 if the duration pre-gate is chosen. |
| S-10       | transcript-injection-guardrail | Screen the transcript for injection before it reaches the LLM | **yes — unblocked 2026-08-06** | **UNBLOCKED — S-09 shipped to production 2026-08-06, so the "plan after S-09" preference is satisfied and this is ready for `/10x-plan`.** The pre-debit stretch S-10 adds a gate to is now settled and deployed: two breaker check points, a metadata-cache lookup, four charging 422 exits and the transcript-cache lookup all sit in front of the debit, and none of them will move again for this reason. **Added 2026-07-31 (user), nice-to-have for the MVP.** **Two decisions are settled and are inputs to planning, not open questions.** (1) **Injection only — subject matter is NOT screened**: YouTube already moderates what it hosts, so re-adjudicating a video's topic duplicates someone else's moderation with worse information and contradicts the product's premise, since summarizing a video *about* a difficult subject is exactly the watch/skip decision the app serves. (2) **Fail-closed**: if the check cannot produce a verdict — unavailable, timed out, errored, unparseable — the generation is refused; unchecked content does not reach the LLM. Availability of the flow yields to the guarantee. **What fail-closed commits the plan to:** a real verdict-producing check is part of the deliverable (prompt-level hardening produces no verdict, so it is defence-in-depth, not the deliverable); "we refused this" and "we could not check this" need distinct user-facing copy, since the second is an outage worth retrying and one the operator needs to see; the check belongs on the pre-debit side so a refusal costs the user no credit and needs no refund path — the Supadata transcript credit is unavoidably already spent by then, the accepted operator-side cost of fail-closed; and precision matters more than under a permissive guard, because a false positive now blocks generation outright, so the refusal rate must be visible to the operator (S-07's ledger is the precedent). **Mechanism and placement deliberately left unspecified** — heuristics vs. a screening model, where the check sits in the request, whether verdicts cache per video, and the latency/cost budget are all deferred to planning, because **S-09 may restructure this exact stretch of the request**. **Updated 2026-07-31:** S-09's design is locked and it does **not** restructure the pipeline (lever A, not the duration pre-gate), so the expiry-date argument for deferring is gone. `Plan after S-09` stands as a preference — both slices add gates to the same pre-debit stretch and are better designed as one sequence — but it is no longer a hard sequencing constraint. See §S-10's Scope notes. **Grounding that does not expire:** the transcript reaches the model in the position it treats as the user's own request, with the app's instructions carried separately — caption text and app instructions compete on equal footing. Main implementation risk: fail-closed makes the check a hard dependency of the core paid flow, so its reliability, timeout budget and false-positive rate are load-bearing rather than best-effort. |

## Open Roadmap Questions

1. **What exactly does "good enough" mean for the 75% criterion?** A lightweight tracking/assessment mechanism is needed (e.g., a periodic self-review). — Owner: user. Block: roadmap-wide (does not block any single slice, but defines when the MVP meets its only success criterion). Source: PRD Open Q2.

## Parked

- **Automatic summary generation for new videos on a channel** — Why parked: PRD §Non-Goals (no channel subscription/monitoring; manual URL addition only).
- **Links to specific video moments (timestamps)** — Why parked: PRD §Non-Goals (a separate feature).
- **Sharing summaries between users** — Why parked: PRD §Non-Goals (data is private; no share mechanism).
- **Sources other than YouTube (podcasts, articles)** — Why parked: PRD §Non-Goals (MVP is YouTube only).
- **Channel character definitions other than informational/educational** — Why parked: PRD §Non-Goals.
- **Automatic channel character selection** — Why parked: PRD §Non-Goals.
- **Editing channel character definitions** — Why parked: PRD §Non-Goals.
- **Mobile applications** — Why parked: PRD §Non-Goals (MVP is web only).
- **S-03: deleting a summary (FR-007)** — Why parked-as-first-to-cut: nice-to-have; with the "speed" goal and the hard 2026-07-31 deadline, it's the first slice to defer if time runs short (remains in `## Slices` as proposed, but is deliberately ranked lowest).

## Done

(Empty on first generation. `/10x-archive` appends an entry here — and flips that item's `Status` to `done` — when a change whose `Change ID` matches is archived. Do NOT pre-populate.)
